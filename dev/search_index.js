var documenterSearchIndex = {"docs":
[{"location":"API/simulation/#Model-specific-functions","page":"Model-specific functions","title":"Model-specific functions","text":"","category":"section"},{"location":"API/simulation/#Data-simulators","page":"Model-specific functions","title":"Data simulators","text":"The philosophy of NeuralEstimators is to cater for any model for which simulation is feasible by allowing users to define their model implicitly through simulated data. However, the following functions have been included as they may be helpful to others, and their source code illustrates how a user could formulate code for their own model.\n\nSee also Distributions.jl for a range of distributions implemented in Julia, and the package RCall for calling R functions within Julia. ","category":"section"},{"location":"API/simulation/#Spatial-point-processes","page":"Model-specific functions","title":"Spatial point processes","text":"","category":"section"},{"location":"API/simulation/#Covariance-functions","page":"Model-specific functions","title":"Covariance functions","text":"These covariance functions may be of use for various models.","category":"section"},{"location":"API/simulation/#Density-functions","page":"Model-specific functions","title":"Density functions","text":"Density functions are not needed in the workflow of NeuralEstimators. However, as part of a series of comparison studies between neural estimators and likelihood-based estimators given in various paper, we have developed the following functions for evaluating the density function for several popular distributions. We include these in NeuralEstimators to cater for the possibility that they may be of use in future comparison studies.","category":"section"},{"location":"API/simulation/#NeuralEstimators.simulategaussian","page":"Model-specific functions","title":"NeuralEstimators.simulategaussian","text":"simulategaussian(L::AbstractMatrix, m = 1)\n\nSimulates m independent and identically distributed realisations from a mean-zero multivariate Gaussian random vector with associated lower Cholesky  factor L. \n\nIf m is not specified, the simulated data are returned as a vector with length equal to the number of spatial locations, n; otherwise, the data are returned as an nxm matrix.\n\nExamples\n\nusing NeuralEstimators, Distances, LinearAlgebra\n\nn = 500\nρ = 0.6\nν = 1.0\nS = rand(n, 2)\nD = pairwise(Euclidean(), S, dims = 1)\nΣ = Symmetric(matern.(D, ρ, ν))\nL = cholesky(Σ).L\nsimulategaussian(L)\n\n\n\n\n\n","category":"function"},{"location":"API/simulation/#NeuralEstimators.simulatepotts","page":"Model-specific functions","title":"NeuralEstimators.simulatepotts","text":"simulatepotts(grid::Matrix{Int}, β)\nsimulatepotts(grid::Matrix{Union{Int, Nothing}}, β)\nsimulatepotts(nrows::Int, ncols::Int, num_states::Int, β)\n\nChequerboard Gibbs sampling from a spatial Potts model with parameter β>0 (see, e.g., Sainsbury-Dale et al., 2025, Sec. 3.3, and the references therein).\n\nApproximately independent simulations can be obtained by setting  nsims > 1 or num_iterations > burn. The degree to which the  resulting simulations can be considered independent depends on the  thinning factor (thin) and the burn-in (burn).\n\nKeyword arguments\n\nnsims = 1: number of approximately independent replicates. \nnum_iterations = 2000: number of MCMC iterations.\nburn = num_iterations: burn-in.\nthin = 10: thinning factor.\n\nExamples\n\nusing NeuralEstimators \n\n## Marginal simulation \nβ = 0.8\nsimulatepotts(10, 10, 3, β)\n\n## Marginal simulation: approximately independent samples \nsimulatepotts(10, 10, 3, β; nsims = 100, thin = 10)\n\n## Conditional simulation \nβ = 0.8\ncomplete_grid   = simulatepotts(100, 100, 3, β)      # simulate marginally \nincomplete_grid = removedata(complete_grid, 0.1)     # randomly remove 10% of the pixels \nimputed_grid    = simulatepotts(incomplete_grid, β)  # conditionally simulate over missing pixels\n\n## Multiple conditional simulations \nimputed_grids   = simulatepotts(incomplete_grid, β; num_iterations = 2000, burn = 1000, thin = 10)\n\n## Recreate Fig. 8.8 of Marin & Robert (2007) “Bayesian Core”\nusing Plots \ngrids = [simulatepotts(100, 100, 2, β) for β ∈ 0.3:0.1:1.2]\nheatmaps = heatmap.(grids, legend = false, aspect_ratio=1)\nPlots.plot(heatmaps...)\n\n\n\n\n\n","category":"function"},{"location":"API/simulation/#NeuralEstimators.simulateschlather","page":"Model-specific functions","title":"NeuralEstimators.simulateschlather","text":"simulateschlather(L::Matrix, m = 1; C = 3.5, Gumbel::Bool = false)\n\nSimulates m independent and identically distributed realisations from Schlather's (2002) max-stable model given the lower Cholesky factor L of the covariance matrix of the underlying Gaussian process. \n\nThe function uses the algorithm for approximate simulation given by Schlather (2002).\n\nIf m is not specified, the simulated data are returned as a vector with length equal to the number of spatial locations, n; otherwise, the data are  returned as an nxm matrix.\n\nKeyword arguments\n\nC = 3.5: a tuning parameter that controls the accuracy of the algorithm. Small C favours computational efficiency, while large C favours accuracy. \nGumbel = true: flag indicating whether the data should be log-transformed from the unit Fréchet scale to the Gumbel scale.\n\nExamples\n\nusing NeuralEstimators, Distances, LinearAlgebra\n\nn = 500\nρ = 0.6\nν = 1.0\nS = rand(n, 2)\nD = pairwise(Euclidean(), S, dims = 1)\nΣ = Symmetric(matern.(D, ρ, ν))\nL = cholesky(Σ).L\nsimulateschlather(L)\n\n\n\n\n\n","category":"function"},{"location":"API/simulation/#NeuralEstimators.maternclusterprocess","page":"Model-specific functions","title":"NeuralEstimators.maternclusterprocess","text":"maternclusterprocess(; λ=10, μ=10, r=0.1, xmin=0, xmax=1, ymin=0, ymax=1, unit_bounding_box=false)\n\nGenerates a realisation from a Matérn cluster process (e.g., Baddeley et al., 2015, Ch. 12). \n\nThe process is defined by a parent homogenous Poisson point process with intensity λ > 0, a mean number of daughter points μ > 0, and a cluster radius r > 0. The simulation is performed over a rectangular window defined by [xmin, xmax] × [ymin, ymax].\n\nIf unit_bounding_box = true, the simulated points will be scaled so that the longest side of their bounding box is equal to one (this may change the simulation window). \n\nSee also the R package spatstat, which provides functions for simulating from a range of point processes and which can be interfaced from Julia using RCall.\n\nExamples\n\nusing NeuralEstimators\n\n# Simulate a realisation from a Matérn cluster process\nS = maternclusterprocess()\n\n# Visualise realisation (requires UnicodePlots)\nusing UnicodePlots\nscatterplot(S[:, 1], S[:, 2])\n\n# Visualise realisations from the cluster process with varying parameters\nn = 250\nλ = [10, 25, 50, 90]\nμ = n ./ λ\nplots = map(eachindex(λ)) do i\n\tS = maternclusterprocess(λ = λ[i], μ = μ[i])\n\tscatterplot(S[:, 1], S[:, 2])\nend\n\n\n\n\n\n","category":"function"},{"location":"API/simulation/#NeuralEstimators.matern","page":"Model-specific functions","title":"NeuralEstimators.matern","text":"matern(h, ρ, ν, σ² = 1)\n\nGiven distance boldsymbolh (h), computes the Matérn covariance function\n\nC(boldsymbolh) = sigma^2 frac2^1 - nuGamma(nu) left(fracboldsymbolhrhoright)^nu K_nu left(fracboldsymbolhrhoright)\n\nwhere ρ is a range parameter, ν is a smoothness parameter, σ² is the marginal variance,  Gamma(cdot) is the gamma function, and K_nu(cdot) is the modified Bessel function of the second kind of order nu.\n\n\n\n\n\n","category":"function"},{"location":"API/simulation/#NeuralEstimators.paciorek","page":"Model-specific functions","title":"NeuralEstimators.paciorek","text":"paciorek(s, r, ω₁, ω₂, ρ, β)\n\nGiven spatial locations s and r, computes the nonstationary covariance function \n\nC(boldsymbols boldsymbolr) = \nboldsymbolSigma(boldsymbols)^14\nboldsymbolSigma(boldsymbolr)^14\nleftfracboldsymbolSigma(boldsymbols) + boldsymbolSigma(boldsymbolr)2right^-12\nC^0big(sqrtQ(boldsymbols boldsymbolr)big) \n\nwhere C^0(h) = exp-(hrho)^32 for range parameter rho  0,  the matrix boldsymbolSigma(boldsymbols) = exp(betaboldsymbols - boldsymbolomega)boldsymbolI  is a kernel matrix (Paciorek and Schervish, 2006)  with scale parameter beta  0 and reference point boldsymbolomega equiv (omega_1 omega_2) in mathbbR^2, and \n\nQ(boldsymbols boldsymbolr) = \n(boldsymbols - boldsymbolr)\nleft(fracboldsymbolSigma(boldsymbols) + boldsymbolSigma(boldsymbolr)2right)^-1\n(boldsymbols - boldsymbolr)\n\nis the squared Mahalanobis distance between boldsymbols and boldsymbolr. \n\nNote that, in practical applications, the reference point boldsymbolomega is often taken to be an estimable parameter rather than fixed and known. \n\n\n\n\n\n","category":"function"},{"location":"API/simulation/#NeuralEstimators.gaussiandensity","page":"Model-specific functions","title":"NeuralEstimators.gaussiandensity","text":"gaussiandensity(Z::V, L::LT) where {V <: AbstractVector, LT <: LowerTriangular}\ngaussiandensity(Z::A, L::LT) where {A <: AbstractArray, LT <: LowerTriangular}\ngaussiandensity(Z::A, Σ::M) where {A <: AbstractArray, M <: AbstractMatrix}\n\nEfficiently computes the density function for Z ~ 𝑁(0, Σ), namely,  \n\n2piboldsymbolSigma^-12 exp-frac12boldsymbolZ^top boldsymbolSigma^-1boldsymbolZ\n\nfor covariance matrix Σ, and where L is lower Cholesky factor of Σ.\n\nThe method gaussiandensity(Z::A, L::LT) assumes that the last dimension of Z contains independent and identically distributed replicates.\n\nIf logdensity = true (default), the log-density is returned.\n\n\n\n\n\n","category":"function"},{"location":"API/simulation/#NeuralEstimators.schlatherbivariatedensity","page":"Model-specific functions","title":"NeuralEstimators.schlatherbivariatedensity","text":"schlatherbivariatedensity(z₁, z₂, ψ₁₂; logdensity = true)\n\nThe bivariate density function (see, e.g., Sainsbury-Dale et al., 2024, Sec. S6.2) for Schlather's (2002) max-stable model, where ψ₁₂ denotes the spatial correlation function evaluated at the locations of observations z₁ and z₂.\n\n\n\n\n\n","category":"function"},{"location":"workflow/examples/#Examples","page":"Examples","title":"Examples","text":"For a very simple example that can be \"copy-paste\", see Quick start on the index page. \n\nBefore proceeding to the main examples, we first load the required packages, the following of which are used throughout these examples:\n\nusing NeuralEstimators\nusing Flux                                  # Julia's deep-learning library\nusing Distributions: InverseGamma, Uniform  # sampling from probability distributions\nusing CairoMakie                            # visualisation\n\nThe following packages will be used in the examples with Gridded data and Irregular spatial data:  \n\nusing Distances                             # distance matrices \nusing Folds                                 # parallel simulation (start Julia with --threads=auto)\nusing LinearAlgebra                         # Cholesky factorisation\n\nThe following packages are used only in the example with Irregular spatial data: \n\nusing GraphNeuralNetworks                   # GNN architecture\nusing Statistics: mean                            \n\nFinally, various GPU backends can be used (see the Flux documentation for details). For instance, to use an NVIDIA GPU in the following examples, simply load CUDA.jl:  \n\nusing CUDA\n\nOnce a GPU package is loaded and a compatible GPU is available, the functions in NeuralEstimators will automatically leverage it to improve computational efficiency while ensuring memory safety via batched operations (GPU usage can be disabled by setting use_gpu = false).","category":"section"},{"location":"workflow/examples/#Univariate-data","page":"Examples","title":"Univariate data","text":"Here, we develop a neural Bayes estimator for boldsymboltheta equiv (mu sigma) from data boldsymbolZ equiv (Z_1 dots Z_m), where each Z_i oversetmathrmiidsim N(mu sigma^2). See Estimators for a list of other classes of estimators available in the package.\n\nWe begin by defining a function to sample parameters from the prior distribution. Assuming prior independence, we adopt the marginal priors mu sim N(0 1) and sigma sim IG(3 1):\n\nfunction sample(K)\n\tμ = randn(K)\n\tσ = rand(InverseGamma(3, 1), K)\n\tθ = vcat(μ', σ')\n\treturn θ\nend\n\nNext, we define the statistical model implicitly through data simulation. Since our data are replicated, the simulated data are stored as a Vector{A}, where each element corresponds to one parameter vector. The type A reflects the multivariate structure of the data. In this example, each replicate Z_1 dots Z_m is univariate, so A is a Matrix with n = 1 row and m columns:\n\nfunction simulate(θ, m)\n    [ϑ[1] .+ ϑ[2] .* randn(1, m) for ϑ in eachcol(θ)]\nend\n\nWe now design our neural network. \n\nAs we are constructing a neural Bayes estimator, the neural network is a mapping mathcalZtoTheta, and the dimensionality of the neural-network output is therefore d equiv textrmdim(Theta) = 2. \n\nSince our data are replicated, we adopt the DeepSets framework, implemented via the type DeepSet. DeepSets consist of two neural networks: an inner network and an outer network. The inner network extracts summary statistics from the data, and its architecture depends on the multivariate structure of the data. For unstructured data (i.e., data without spatial or temporal correlation within each replicate), we use a multilayer perceptron (MLP). The input dimension matches the dimensionality of each data replicate, while the output dimension corresponds to the number of summary statistics appropriate for the model (a common choice is d). The outer network maps the learned summary statistics to the output space (here, the parameter space, Theta). The outer network is always an MLP. \n\nBelow is an example of a DeepSets architecture for neural Bayes estimation in this example. Note that many models have parameter constraints (e.g., variance and range parameters that must be strictly positive). These constraints can be incorporated in the final layer of the neural network by choosing appropriate activation functions for each parameter. Here, we enforce the constraint sigma  0 by applying the softplus activation function in the final layer of the outer network, ensuring that all parameter estimates are valid. For some additional ways to constrain parameter estimates, see Output layers. \n\nn = 1    # dimension of each data replicate (univariate)\nd = 2    # dimension of the parameter vector θ\nw = 128  # width of each hidden layer \n\n# Final layer has output dimension d and enforces parameter constraints\nfinal_layer = Parallel(\n    vcat,\n    Dense(w, 1, identity),     # μ ∈ ℝ\n    Dense(w, 1, softplus)      # σ > 0\n)\n\n# Inner and outer networks\nψ = Chain(Dense(n, w, relu), Dense(w, d, relu))    \nϕ = Chain(Dense(d, w, relu), final_layer)          \n\n# Combine into a DeepSet\nnetwork = DeepSet(ψ, ϕ)\n\nWe then initialise the neural Bayes estimator by wrapping the neural network in a PointEstimator: \n\nestimator = PointEstimator(network)\n\nNext, we train the estimator using train(), here using the default mean-absolute-error loss, so that the resulting neural Bayes estimator approximates the marginal posterior medians. We'll train the estimator using m=50 independent replicates per parameter configuration. Below, we pass our user-defined functions for sampling parameters and simulating data, but one may also pass parameter or data instances, which will be held fixed during training:\n\nm = 50\nestimator = train(estimator, sample, simulate, simulator_args = m)\n\nOne may wish to save a trained estimator and load it in a later session: see Saving and loading neural estimators for details on how this can be done. \n\nThe function assess() can be used to assess the trained estimator. Parametric and non-parametric bootstrap estimates can be obtained via bootstrap(), with corresponding confidence intervals computed using interval(). Additionally, non-parametric bootstrap-based uncertainty quantification can be included in the assessment stage through the keyword argument probs:\n\nθ_test = sample(1000)\nZ_test = simulate(θ_test, m)\nassessment = assess(estimator, θ_test, Z_test, probs = [0.025, 0.975])\n\nThe resulting Assessment object contains the sampled parameters, the corresponding point estimates, and the corresponding lower and upper bounds of the bootstrap intervals. This object can be used to compute various diagnostics and to visualise the neural point estimates and bootstrap intervals vs the true parameter value:  \n\nbias(assessment)      # μ = 0.002, σ = 0.017\nrmse(assessment)      # μ = 0.086, σ = 0.078\nrisk(assessment)      # μ = 0.055, σ = 0.056\nplot(assessment)\n\n(Image: Univariate Gaussian example: Estimates vs. truth)\n\nAs an alternative form of uncertainty quantification with neural Bayes estimators, one may approximate a set of marginal posterior quantiles by training a neural Bayes estimator under the quantile loss function, which allows one to generate approximate marginal posterior credible intervals. This is facilitated with IntervalEstimator which, by default, targets 95% central credible intervals:\n\nq̂ = IntervalEstimator(network)\nq̂ = train(q̂, sample, simulate, simulator_args = m)\n\nThe resulting posterior credible-interval estimator can also be assessed using assess(). Often, these intervals have better coverage than bootstrap-based intervals.\n\nOnce an estimator is deemed to be well calibrated, it may be applied to observed data (below, we use simulated data as a substitute for observed data):\n\nθ = sample(1)                       # true parameters\nZ = simulate(θ, m)                  # \"observed\" data\nestimate(estimator, Z)              # point estimate\ninterval(bootstrap(estimator, Z))   # 95% non-parametric bootstrap intervals\ninterval(q̂, Z)                      # 95% marginal posterior credible intervals","category":"section"},{"location":"workflow/examples/#Gridded-data","page":"Examples","title":"Gridded data","text":"For data collected over a regular grid, neural estimators are typically based on a convolutional neural network (CNN; see, e.g., Dumoulin and Visin, 2016). \n\nWhen using CNNs with NeuralEstimators, each data set must be stored as a multi-dimensional array. The penultimate dimension stores the so-called \"channels\" (this dimension is singleton for univariate processes, two for bivariate processes), while the final dimension stores independent replicates. For example, to store 50 independent replicates of a bivariate spatial process measured over a 10times15 grid, one would construct an array of dimension 10times15times2times50.\n\nFor illustration, here we develop a neural Bayes estimator for the (univariate) spatial Gaussian process model with exponential covariance function and unknown range parameter theta  0. The spatial domain is taken to be the unit square, we simulate data on a regular square grid of size n = 16^2 = 256, and we adopt the prior theta sim U(0 05). \n\nSimulation from Gaussian processes typically involves the computation of an expensive intermediate object, namely, the Cholesky factor of a covariance matrix. Storing intermediate objects can enable the fast simulation of new data sets when the parameters are held fixed. Hence, in this example, we define a custom type Parameters subtyping AbstractParameterSet for storing the matrix of parameters and the corresponding Cholesky factors: \n\nstruct Parameters{T} <: AbstractParameterSet\n\tθ::Matrix{T}\n\tL\nend\n\nFurther, we define two constructors for our custom type: one that accepts an integer K, and another that accepts a dtimes K matrix of parameters. The former constructor will be useful during the training stage for sampling from the prior distribution, while the latter constructor will be useful for parametric bootstrap (since this involves repeated simulation from the fitted model):\n\nfunction sample(K::Integer)\n\t# Sample parameters from the prior \n\tθ = 0.5 * rand(1, K)\n\n\t# Pass to matrix constructor\n\tParameters(θ)\nend\n\nfunction Parameters(θ::Matrix)\n\t# Spatial locations, a 16x16 grid over the unit square\n\tpts = range(0, 1, length = 16)\n\tS = expandgrid(pts, pts)\n\n\t# Distance matrix, covariance matrices, and Cholesky factors\n\tD = pairwise(Euclidean(), S, dims = 1)\n\tK = size(θ, 2)\n\tL = Folds.map(1:K) do k\n\t\tΣ = exp.(-D ./ θ[k])\n\t\tcholesky(Symmetric(Σ)).L\n\tend\n\n\tParameters(θ, L)\nend\n\nNext, we define the model simulator, which returns simulated data as a four-dimensional array (see Simulating data for an overview of common data formats): \n\nfunction simulate(parameters::Parameters, m = 1) \n\tZ = Folds.map(parameters.L) do L\n\t\tn = size(L, 1)\n\t\tz = L * randn(n, m)\n\t\tz = reshape(z, 16, 16, 1, m) \n\t\tz\n\tend\n\tZ\nend\n\nA possible neural-network architecture is as follows. Note that deeper architectures that employ residual connections (see ResidualBlock) often lead to improved performance, and certain pooling layers (e.g., GlobalMeanPool) allow the neural network to accommodate grids of varying dimension; for further discussion and an illustration, see Sainsbury-Dale et al. (2025, Sec. S3, S4). \n\n# Inner network \nψ = Chain(\n      Conv((3, 3), 1 => 32, relu),   # 3x3 convolutional filter, 1 input channel to 32 output channels\n      MaxPool((2, 2)),               # 2x2 max pooling for dimension reduction\n      Conv((3, 3), 32 => 64, relu),  # 3x3 convolutional filter, 32 input channels to 64 output channels\n      MaxPool((2, 2)),               # 2x2 max pooling for dimension reduction\n      Flux.flatten                   # flatten output to feed into a fully connected layer\n  )\n\n# Outer network \nϕ = Chain(Dense(256, 64, relu), Dense(64, 1))\n\n# DeepSet object\nnetwork = DeepSet(ψ, ϕ)\n\nAbove, we embedded our CNN within the DeepSets framework to accommodate scenarios involving replicated spatial data (e.g., when fitting models for spatial extremes). However, the package allows users to define the neural network using any Flux model. Since this example does not include independent replicates, one could instead store each simulated data set in the final dimension of a four-dimensional array, and then use a generic CNN architecture.  \n\nNext, we initialise a point estimator and a posterior credible-interval estimator:\n\nθ̂ = PointEstimator(network)\nq̂ = IntervalEstimator(network)\n\nNow we train the estimators, here using fixed parameter instances to avoid repeated Cholesky factorisations (see Storing expensive intermediate objects for data simulation and On-the-fly and just-in-time simulation for further discussion):\n\nK = 10000 # number of training parameter vectors\nθ_train = sample(K)\nθ_val   = sample(K ÷ 10)\nθ̂ = train(θ̂, θ_train, θ_val, simulate)\nq̂ = train(q̂, θ_train, θ_val, simulate)\n\nOnce the estimators have been trained, we assess them using empirical simulation-based methods:\n\nθ_test = sample(1000)\nZ_test = simulate(θ_test)\nassessment = assess([θ̂, q̂], θ_test, Z_test)\n\nbias(assessment)       # 0.005\nrmse(assessment)       # 0.032\ncoverage(assessment)   # 0.953\nplot(assessment)       \n\n(Image: Gridded spatial Gaussian process example: Estimates vs. truth)\n\nFinally, we can apply our estimators to observed data:\n\nθ = Parameters(Matrix([0.1]'))         # true parameter\nZ = simulate(θ)                        # \"observed\" data\nestimate(θ̂, Z)                         # point estimate: 0.11\ninterval(q̂, Z)                         # 95% marginal posterior credible interval: [0.08, 0.16]\n\nNote that missing data (e.g., due to cloud cover) can be accommodated using the missing-data methods implemented in the package.","category":"section"},{"location":"workflow/examples/#Irregular-spatial-data","page":"Examples","title":"Irregular spatial data","text":"To cater for spatial data collected over arbitrary spatial locations, one may construct a neural estimator with a graph neural network (GNN; see Sainsbury-Dale, Zammit-Mangion, Richards, and Huser, 2025). The overall workflow remains as given in previous examples, with two key additional considerations.\n\nFirst, if inference is to be made from a single spatial data set collected before constructing estimator, training data can be simulated using the observed spatial locations, which can be treated as fixed and known. However, if the estimator is intended for application to multiple spatial data sets with varying spatial configurations, it should be trained on a diverse set of spatial configurations. These configurations can be sampled during training, possibly using a spatial point process such as maternclusterprocess(). \n\nSecond, the spatial data should be stored as a graph, which can be achieved using spatialgraph().\n\nFor illustration, we again consider a spatial Gaussian process model with exponential covariance function, and we define a type for storing expensive intermediate objects needed for data simulation. In this example, these objects include Cholesky factors, and spatial graphs which store the adjacency matrices needed to perform graph convolutions: \n\nstruct Parameters <: AbstractParameterSet\n\tθ::Matrix      # true parameters  \n\tL              # Cholesky factors\n\tg              # spatial graphs\n\tS              # spatial locations \nend\n\nAgain, we define two constructors, which will be convenient for sampling parameters from the prior during training and assessment, and for parametric bootstrap sampling when making inferences from observed data:\n\nfunction sample(K::Integer)\n\t# Sample parameters from the prior \n\tθ = 0.5 * rand(1, K)\n\n\t# Sample spatial configurations from Matern cluster process on [0, 1]²\n\tn = rand(200:300, K)\n\tλ = rand(Uniform(10, 50), K)\n\tS = [maternclusterprocess(λ = λ[k], μ = n[k]/λ[k]) for k ∈ 1:K]\n\n\t# Pass to constructor\n\tParameters(θ, S)\nend\n\nfunction Parameters(θ::Matrix, S)\n\t# Compute covariance matrices and Cholesky factors \n\tL = Folds.map(axes(θ, 2)) do k\n\t\tD = pairwise(Euclidean(), S[k], dims = 1)\n\t\tΣ = Symmetric(exp.(-D ./ θ[k]))\n\t\tcholesky(Σ).L\n\tend\n\n\t# Construct spatial graphs\n\tg = spatialgraph.(S)\n\n\t# Store in Parameters object\n\tParameters(θ, L, g, S)\nend\n\nNext, we define a function for simulating from the model given an object of type Parameters. The code below enables simulation of an arbitrary number of independent replicates m, and one may provide a single integer for m, or any object that can be sampled using rand(m, K) (e.g., an integer range or some distribution over the possible sample sizes):\n\nfunction simulate(parameters::Parameters, m)\n\tK = size(parameters, 2)\n\tm = rand(m, K)\n\tmap(1:K) do k\n\t\tL = parameters.L[k]\n\t\tg = parameters.g[k]\n\t\tn = size(L, 1)\n\t\tZ = L * randn(n, m[k])      \n\t\tspatialgraph(g, Z)            \n\tend\nend\nsimulate(parameters::Parameters, m::Integer = 1) = simulate(parameters, range(m, m))\n\nNext, we construct our GNN architecture. Here, we use an architecture tailored to isotropic spatial dependence models; for further details, see Sainsbury-Dale et al. (2025, Sec. 2.2). We also employ a sparse approximation of the empirical variogram as an expert summary statistic (Gerber and Nychka, 2021).\n\nIn this example our goal is to construct a point estimator, however any other kind of estimator (see Estimators) can be constructed by simply substituting the appropriate estimator class in the final line below:\n\n# Spatial weight functions: continuous surrogates for 0-1 basis functions \nh_max = 0.15 # maximum distance to consider \nq = 10       # output dimension of the spatial weights\nw = KernelWeights(h_max, q)\n\n# Propagation module\npropagation = Chain(\n\tSpatialGraphConv(1 => q, relu, w = w, w_out = q),\n\tSpatialGraphConv(q => q, relu, w = w, w_out = q)\n)\n\n# Readout module\nreadout = GlobalPool(mean)\n\n# Inner network\nψ = GNNSummary(propagation, readout)\n\n# Expert summary statistics, the empirical variogram\nS = NeighbourhoodVariogram(h_max, q)\n\n# Outer network\nϕ = Chain(\n\tDense(2q => 128, relu), \n\tDense(128 => 128, relu), \n\tDense(128 => 1, identity)\n)\n\n# DeepSet object\nnetwork = DeepSet(ψ, ϕ; S = S)\n\n# Point estimator\nestimator = PointEstimator(network)\n\nNext, we train the estimator. \n\nm = 1\nK = 5000\nθ_train = sample(K)\nθ_val   = sample(K÷5)\nestimator = train(estimator, θ_train, θ_val, simulate, simulator_args = m, epochs = 10)\n\nNote that the computations in GNNs are performed in parallel, making them particularly well-suited for GPUs, which typically contain thousands of cores. If you have access to an NVIDIA GPU, you can utilise it by simply loading the Julia package CUDA. \n\nNext, we assess our trained estimator: \n\nθ_test = sample(1000)\nZ_test = simulate(θ_test, m)\nassessment = assess(estimator, θ_test, Z_test)\nbias(assessment)   \nrmse(assessment)    \nrisk(assessment)   \nplot(assessment)   \n\n(Image: Estimates from a graph neural network (GNN) based neural Bayes estimator)\n\nFinally, once the estimator has been assessed, it may be applied to observed data, with bootstrap-based uncertainty quantification facilitated by bootstrap() and interval(). Note that, since the estimator was trained using spatial configurations in the unit square 0 1 times 0 1, the spatial coordinates of observed data should be scaled by a common factor such that they are also contained within this unit square; estimates of any range parameters are then scaled back accordingly. Below, we use simulated data as a substitute for observed data:\n\nparameters = sample(1)             # sample a parameter vector and spatial locations              \nθ = parameters.θ                   # true parameters\nS = parameters.S                   # \"observed\" locations\nZ = simulate(parameters)           # \"observed\" data    \nθ̂ = estimate(estimator, Z)         # point estimate\nps = Parameters(θ̂, S)              # construct Parameters object from point estimate\nbs = bootstrap(estimator, ps, simulate, m)  # parametric bootstrap estimates\ninterval(bs)                       # parametric bootstrap confidence interval              ","category":"section"},{"location":"methodology/#Methodology","page":"Methodology","title":"Methodology","text":"Here, we provide an overview of the amortised neural inferential methods supported by the package, which include neural Bayes estimators, neural posterior estimators, and neural ratio estimators. For further details on each of these methods and amortised neural inference more broadly, see the review paper by Zammit-Mangion et al. (2025) and the references therein.\n\nNotation: We denote model parameters of interest by boldsymboltheta equiv (theta_1 dots theta_d) in Theta, where Theta subseteq mathbbR^d is the parameter space. We denote data by boldsymbolZ equiv (Z_1 dots Z_n) in mathcalZ, where mathcalZ subseteq mathbbR^n is the sample space. We denote neural-network parameters by boldsymbolgamma. For simplicity, we assume that all measures admit densities with respect to the Lebesgue measure. We use pi(cdot) to denote the prior density function of the parameters. The input argument to a generic density function p(cdot) serves to specify both the random variable associated with the density and its evaluation point.","category":"section"},{"location":"methodology/#Neural-Bayes-estimators","page":"Methodology","title":"Neural Bayes estimators","text":"The goal of parametric point estimation is to estimate boldsymboltheta from data boldsymbolZ using an estimator, hatboldsymboltheta  mathcalZtoTheta. Estimators can be constructed intuitively within a decision-theoretic framework based on average-risk optimality. Specifically, consider a loss function L Theta times Theta to 0 infty). Then the Bayes risk of the estimator hatboldsymboltheta(cdot) is  \n\nint_Theta int_mathcalZ  L(boldsymboltheta hatboldsymboltheta(boldsymbolZ))p(boldsymbolZ mid boldsymboltheta) pi(boldsymboltheta) textrmd boldsymbolZ textrmdboldsymboltheta  \n\nAny minimiser of the Bayes risk is said to be a Bayes estimator with respect to L(cdot cdot) and pi(cdot). \n\nBayes estimators are functionals of the posterior distribution (e.g., the Bayes estimator under quadratic loss is the posterior mean), and are therefore often unavailable in closed form. A way forward is to assume a flexible parametric function for hatboldsymboltheta(cdot), and to optimise the parameters within that function in order to approximate the Bayes estimator. Neural networks are ideal candidates, since they are universal function approximators, and because they are fast to evaluate. Let hatboldsymboltheta_boldsymbolgamma  mathcalZtoTheta denote a neural network parameterised by boldsymbolgamma. Then a Bayes estimator may be approximated by hatboldsymboltheta_boldsymbolgamma^*(cdot), where \n\nboldsymbolgamma^* equiv undersetboldsymbolgammamathrmargmin frac1K sum_k = 1^K L(boldsymboltheta^(k) hatboldsymboltheta_boldsymbolgamma(boldsymbolZ^(k)))\n\nwith boldsymboltheta^(k) sim pi(boldsymboltheta) and, independently for each k, boldsymbolZ^(k) sim p(boldsymbolZ mid  boldsymboltheta^(k)). The process of obtaining boldsymbolgamma^* is referred to as \"training the network\", and this can be performed efficiently using back-propagation and stochastic gradient descent. The trained neural network hatboldsymboltheta_boldsymbolgamma^*(cdot) approximately minimises the Bayes risk, and therefore it is called a neural Bayes estimator (Sainsbury-Dale at al., 2024). \n\nOnce trained, a neural Bayes estimator can be applied repeatedly to observed data (whose structure conforms with the chosen neural-network architecture) at a fraction of the computational cost of conventional inferential methods. It is therefore ideal to use a neural Bayes estimator in settings where inference needs to be made repeatedly; in this case, the initial training cost is said to be amortised over time. ","category":"section"},{"location":"methodology/#Uncertainty-quantification-with-neural-Bayes-estimators","page":"Methodology","title":"Uncertainty quantification with neural Bayes estimators","text":"Uncertainty quantification with neural Bayes estimators often proceeds through the bootstrap distribution (e.g., Lenzi et al., 2023; Richards et al., 2024; Sainsbury-Dale et al., 2024). Bootstrap-based approaches are particularly attractive when nonparametric bootstrap is possible (e.g., when the data are independent replicates), or when simulation from the fitted model is fast, in which case parametric bootstrap is also computationally efficient. However, these conditions are not always met and, although bootstrap-based approaches are often considered to be fairly accurate and favourable to methods based on asymptotic normality, there are situations where bootstrap procedures are not reliable (see, e.g., Canty et al., 2006, pg. 6). \n\nAlternatively, by leveraging ideas from (Bayesian) quantile regression, one may construct a neural Bayes estimator that approximates a set of marginal posterior quantiles (Fisher et al., 2023; Sainsbury-Dale et al., 2025), which can then be used to construct credible intervals for each parameter. Inference then remains fully amortised since, once the estimators are trained, both point estimates and credible intervals can be obtained with virtually zero computational cost. Specifically, posterior quantiles can be targeted by training a neural Bayes estimator under the loss function\n\nL(boldsymboltheta hatboldsymboltheta tau) equiv sum_j=1^d (hattheta_j - theta_j)mathbbI(hattheta_j - theta_j) - tau quad 0  tau  1\n\nwhere mathbbI(cdot) denotes the indicator function, since the Bayes estimator under this loss function is the vector of marginal posterior tau-quantiles (Sainsbury-Dale et al., 2025, Sec. 2.2.4). ","category":"section"},{"location":"methodology/#Neural-posterior-estimators","page":"Methodology","title":"Neural posterior estimators","text":"We now describe amortised approximate posterior inference through the minimisation of an expected Kullback–Leibler (KL) divergence. Throughout, we let q(boldsymboltheta boldsymbolkappa) denote a parametric approximation to the posterior distribution p(boldsymboltheta mid boldsymbolZ), where the approximate-distribution parameters boldsymbolkappa belong to a space mathcalK. \n\nWe first consider the non-amortised case, where the optimal parameters boldsymbolkappa^* for a single data set boldsymbolZ are found by minimising the KL divergence between p(boldsymboltheta mid boldsymbolZ) and q(boldsymboltheta boldsymbolkappa): \n\n  boldsymbolkappa^* \n  equiv undersetboldsymbolkappamathrmargmin textrmKLp(boldsymboltheta mid boldsymbolZ)    q(boldsymboltheta boldsymbolkappa)\n  = undersetboldsymbolkappamathrmargmin -int_Theta log q(boldsymboltheta boldsymbolkappa) p(boldsymboltheta mid boldsymbolZ) textrmdboldsymboltheta\n  \n\nThe resulting approximate posterior q(boldsymboltheta boldsymbolkappa^*) targets the true posterior in the sense that the KL divergence is zero if and only if q(boldsymboltheta boldsymbolkappa^*) = p(boldsymboltheta mid boldsymbolZ) for all boldsymboltheta in Theta. However, solving this optimisation problem is often computationally demanding even for a single data set boldsymbolZ, and solving it for many different data sets can be computationally prohibitive. The optimisation problem can be amortised by treating the parameters boldsymbolkappa as a function boldsymbolkappa  mathcalZ to mathcalK, and then choosing the function boldsymbolkappa^*(cdot) that minimises an expected KL divergence: \n\nboldsymbolkappa^*(cdot) equiv undersetboldsymbolkappa(cdot)mathrmargmin mathbbE_boldsymbolZtextrmKLp(boldsymboltheta mid boldsymbolZ)    q(boldsymboltheta boldsymbolkappa(boldsymbolZ))\n\nIn practice, we approximate boldsymbolkappa^*(cdot) using a neural network, boldsymbolkappa_boldsymbolgamma  mathcalZ to mathcalK, which is parameterised by boldsymbolgamma and trained by minimising a Monte Carlo approximation of the expected KL divergence above: \n\nboldsymbolgamma^* equiv undersetboldsymbolgammamathrmargmin -sum_k=1^K log q(boldsymboltheta^(k) boldsymbolkappa_boldsymbolgamma(boldsymbolZ^(k)))\n\nOnce trained, the neural network boldsymbolkappa_boldsymbolgamma^*(cdot) may be used to estimate the optimal approximate-distribution parameters boldsymbolkappa^* given data boldsymbolZ at almost no computational cost. The neural network boldsymbolkappa_boldsymbolgamma^*(cdot), together with the corresponding approximate distribution q(cdot boldsymbolkappa), is collectively referred to as a neural posterior estimator. \n\nThere are numerous options for the approximate distribution q(cdot boldsymbolkappa). For instance, q(cdotboldsymbolkappa) can be modelled as a Gaussian distribution (e.g., Chan et al., 2018, where the parameters boldsymbolkappa = (boldsymbolmu textrmvech(boldsymbolL)) consist of a d-dimensional mean parameter boldsymbolmu and the d(d+1)2 non-zero elements of the lower Cholesky factor boldsymbolL of a covariance matrix, and the half-vectorisation operator textrmvech(cdot) vectorises the lower triangle of its matrix argument. For further flexibility, one may consider trans-Gaussian distributions (e.g., Maceda et al., 2024) or Gaussian mixtures (e.g., Papamakarios & Murray, 2016), the latter of which is implemented in the package as GaussianMixture. \n\nAnother widely adopted approach to modelling q(cdot boldsymbolkappa) is through the use of normalising flows (e.g., Ardizzone et al., 2019; Radev et al., 2022), excellent reviews for which are given by Kobyzev et al. (2020) and Papamakarios (2021). A particularly popular class of normalising flow is the affine coupling flow (e.g., Dinh et al., 2016; Kingma & Dhariwal, 2018; Ardizzone et al., 2019), which are universal density approximators (Teshima et al., 2020) and are implemented in the package as NormalisingFlow. ","category":"section"},{"location":"methodology/#Neural-ratio-estimators","page":"Methodology","title":"Neural ratio estimators","text":"Finally, we describe amortised inference by approximation of the likelihood-to-evidence ratio, \n\nr(boldsymbolZ boldsymboltheta) equiv p(boldsymbolZ mid boldsymboltheta)p(boldsymbolZ)\n\nwhere p(boldsymbolZ mid boldsymboltheta) is the likelihood and p(boldsymbolZ) is the marginal likelihood (also known as the model evidence). \n\nThe likelihood-to-evidence ratio is ubiquitous in statistical inference. For example, likelihood ratios of the form p(boldsymbolZmid boldsymboltheta_0)p(boldsymbolZmid boldsymboltheta_1)=r(boldsymbolZ boldsymboltheta_0)r(boldsymbolZ boldsymboltheta_1) are central to hypothesis testing and model comparison, and naturally appear in the transition probabilities of most standard MCMC algorithms used for Bayesian inference. Further, since the likelihood-to-evidence ratio is a prior-free quantity, its approximation facilitates Bayesian inference in applications where one requires multiple fits of the model under different prior distributions. \n\nUnlike the methods discussed earlier, the likelihood-to-evidence ratio might not immediately seem like a quantity well-suited for approximation by neural networks, which are trained by minimising empirical risk functions. However, this ratio emerges naturally as a simple transformation of the optimal solution to a standard binary classification problem, derived through the minimisation of an average risk. Specifically, consider a binary classifier c(boldsymbolZ boldsymboltheta) that distinguishes dependent data-parameter pairs (boldsymbolZ boldsymboltheta) sim p(boldsymbolZ boldsymboltheta) with class labels Y=1 from independent data-parameter pairs (tildeboldsymbolZ tildeboldsymboltheta) sim p(boldsymbolZ)p(boldsymboltheta) with class labels Y=0, and where the classes are balanced. Here, p(boldsymboltheta) denotes an arbitrary \"proposal\" distribution for boldsymboltheta that does not, in general, coincide with the prior distribution (see below). Then, the Bayes classifier under binary cross-entropy loss is defined as \n\nbeginaligned\nc^*(cdot cdot) \nequiv\nundersetc(cdot cdot)mathrmargmin sum_yin0 1 textrmPr(Y = y)  int_Thetaint_mathcalZL_textrmBCEy c(boldsymbolZ boldsymboltheta)p(boldsymbolZ boldsymboltheta mid Y = y)textrmd boldsymbolZ textrmd boldsymboltheta\n=\nundersetc(cdot cdot)mathrmargmin - int_Thetaint_mathcalZBiglogc(boldsymbolZ boldsymboltheta)p(boldsymbolZ boldsymboltheta)  + log1 - c(boldsymbolZ boldsymboltheta)p(boldsymbolZ)p(boldsymboltheta) Bigtextrmd boldsymbolZ textrmd boldsymboltheta\nendaligned\n\nwhere L_textrmBCE(y c) equiv -ylog(c) - (1 - y) log(1 - c). It can be shown (e.g., Hermans et al., 2020, App. B)  that the Bayes classifier is given by \n\nc^*(boldsymbolZ boldsymboltheta) = fracp(boldsymbolZ boldsymboltheta)p(boldsymbolZ boldsymboltheta) + p(boldsymboltheta)p(boldsymbolZ) quad boldsymbolZ in mathcalZ boldsymboltheta in Theta\n\nand, hence,\n\nr(boldsymbolZ boldsymboltheta) = fracc^*(boldsymbolZ boldsymboltheta)1 - c^*(boldsymbolZ boldsymboltheta) quad boldsymbolZ in mathcalZ boldsymboltheta in Theta\n\nThis connection links the likelihood-to-evidence ratio to the average-risk-optimal solution of a standard binary classification problem, and consequently provides a foundation for approximating the ratio using neural networks. Specifically, let c_boldsymbolgamma mathcalZ times Theta to (0 1) denote a neural network parametrised by boldsymbolgamma. Then the Bayes classifier may be approximated by c_boldsymbolgamma^*(cdot cdot), where \n\n boldsymbolgamma^* equiv undersetboldsymbolgammamathrmargmin -sum_k=1^K Biglogc_boldsymbolgamma(boldsymbolZ^(k) boldsymboltheta^(k)) +  log1 - c_boldsymbolgamma(boldsymbolZ^(sigma(k)) boldsymboltheta^(k)) Big\n\nwith each boldsymboltheta^(k) sampled independently from a \"proposal\" distribution p(boldsymboltheta), boldsymbolZ^(k) sim p(boldsymbolZ mid boldsymboltheta^(k)), and sigma(cdot) a random permutation of 1 dots K. The proposal distribution p(boldsymboltheta) does not necessarily correspond to the prior distribution pi(boldsymboltheta), which is specified in the downstream inference algorithm (see below). In theory, any p(boldsymboltheta) with support over Theta can be used. However, with finite training data, the choice of p(boldsymboltheta) is important, as it determines where the parameters boldsymboltheta^(k) are most densely sampled and, hence, where the neural network c_boldsymbolgamma^*(cdot cdot) best approximates the Bayes classifier. Further, since neural networks are only reliable within the support of their training samples, a p(boldsymboltheta) lacking full support over Theta essentially acts as a \"soft prior\". \n\nOnce the neural network is trained, r_boldsymbolgamma^*(boldsymbolZ boldsymboltheta) equiv c_boldsymbolgamma^*(boldsymbolZ boldsymboltheta)1 - c_boldsymbolgamma^*(boldsymbolZ boldsymboltheta)^-1, boldsymbolZ in mathcalZ boldsymboltheta in Theta, may be used to quickly approximate the likelihood-to-evidence ratio, and therefore it is called a neural ratio estimator. \n\nInference based on a neural ratio estimator may proceed in a frequentist setting via maximum likelihood and likelihood ratios (e.g., Walchessen et al., 2024), and in a Bayesian setting by facilitating the computation of transition probabilities in Hamiltonian Monte Carlo and MCMC algorithms (e.g., Hermans et al., 2020). Further, an approximate posterior distribution can be obtained via the identity p(boldsymboltheta mid boldsymbolZ) = pi(boldsymboltheta) r(boldsymboltheta boldsymbolZ), and sampled from using standard sampling techniques (e.g., Thomas et al., 2022).","category":"section"},{"location":"API/#Index","page":"Index","title":"Index","text":"","category":"section"},{"location":"workflow/advancedusage/#Advanced-usage","page":"Advanced usage","title":"Advanced usage","text":"","category":"section"},{"location":"workflow/advancedusage/#Saving-and-loading-neural-estimators","page":"Advanced usage","title":"Saving and loading neural estimators","text":"In regards to saving and loading, neural estimators behave in the same manner as regular Flux models. Therefore, the examples and recommendations outlined in the Flux documentation also apply directly to neural estimators. For example, to save the model state of the neural estimator estimator, run:\n\nusing Flux\nusing BSON: @save, @load\nmodel_state = Flux.state(estimator)\n@save \"estimator.bson\" model_state\n\nThen, to load it in a new session, one may initialise a neural estimator with the same architecture used previously, and load the saved model state as follows:\n\n@load \"estimator.bson\" model_state\nFlux.loadmodel!(estimator, model_state)\n\nIt is also straightforward to save the entire neural estimator, including its architecture (see here). However, the first approach outlined above is recommended for long-term storage.\n\nFor convenience, the function train() allows for the automatic saving of the model state during the training stage, via the argument savepath.","category":"section"},{"location":"workflow/advancedusage/#Storing-expensive-intermediate-objects-for-data-simulation","page":"Advanced usage","title":"Storing expensive intermediate objects for data simulation","text":"Parameters sampled from the prior distribution may be stored in two ways. Most simply, they can be stored as a d times K matrix, where d is the number of parameters in the model and K is the number of parameter vectors sampled from the prior distribution. Alternatively, they can be stored in a user-defined subtype of AbstractParameterSet, whose only requirement is a field θ that stores the d times K matrix of parameters. With this approach, one may store computationally expensive intermediate objects, such as Cholesky factors, for later use when conducting \"on-the-fly\" simulation, which is discussed below.","category":"section"},{"location":"workflow/advancedusage/#On-the-fly-and-just-in-time-simulation","page":"Advanced usage","title":"On-the-fly and just-in-time simulation","text":"When data simulation is (relatively) computationally inexpensive, the training data set, mathcalZ_texttrain, can be simulated continuously during training, a technique coined \"simulation-on-the-fly\". Regularly refreshing mathcalZ_texttrain leads to lower out-of-sample error and to a reduction in overfitting. This strategy therefore facilitates the use of larger, more representationally-powerful networks that are prone to overfitting when mathcalZ_texttrain is fixed. Further, this technique allows for data to be simulated \"just-in-time\", in the sense that they can be simulated in small batches, used to train the neural estimator, and then removed from memory. This can substantially reduce pressure on memory resources, particularly when working with large data sets.\n\nOne may also regularly refresh the set vartheta_texttrain of parameter vectors used during training, and doing so leads to similar benefits. However, fixing vartheta_texttrain allows computationally expensive terms, such as Cholesky factors when working with Gaussian process models, to be reused throughout training, which can substantially reduce the training time for some models. Hybrid approaches are also possible, whereby the parameters (and possibly the data) are held fixed for several epochs (i.e., several passes through the training set when performing stochastic gradient descent) before being refreshed.\n\nThe above strategies are facilitated with various methods of train().","category":"section"},{"location":"workflow/advancedusage/#Feature-scaling","page":"Advanced usage","title":"Feature scaling","text":"It is important to ensure that the data passed through the neural network are on a reasonable numerical scale, since values with very large absolute value can lead to numerical instability during training (e.g., exploding gradients). \n\nA relatively simply way to achieve this is by including a transformation in the first layer of the neural network. For example, if the data have positive support, one could define the neural network with the first layer applying a log transformation:\n\nnetwork = Chain(z -> log.(1 + z), ...)\n\nIf the data are not strictly positive, one may consider the following signed transformation:\n\nnetwork = Chain(z -> sign.(z) .* log.(1 .+ abs.(z)), ...)\n\nA simple preprocessing layer or transformation pipeline such as this can make a significant difference in performance and stability. See feature scaling for further discussion and possible approaches. ","category":"section"},{"location":"workflow/advancedusage/#Regularisation","page":"Advanced usage","title":"Regularisation","text":"The term regularisation refers to a variety of techniques aimed to reduce overfitting when training a neural network, primarily by discouraging complex models.\n\nA popular regularisation technique is known as dropout, implemented in Flux's Dropout layer. Dropout involves temporarily dropping (\"turning off\") a randomly selected set of neurons (along with their connections) at each iteration of the training stage, which results in a computationally-efficient form of model (neural-network) averaging (Srivastava et al., 2014).\n\nAnother class of regularisation techniques involve modifying the loss function. For instance, L₁ regularisation (sometimes called lasso regression) adds to the loss a penalty based on the absolute value of the neural-network parameters. Similarly, L₂ regularisation (sometimes called ridge regression) adds to the loss a penalty based on the square of the neural-network parameters. Note that these penalty terms are not functions of the data or of the statistical-model parameters that we are trying to infer. These regularisation techniques can be implemented straightforwardly by providing a custom optimiser to train() that includes a SignDecay object for L₁ regularisation, or a WeightDecay object for L₂ regularisation. See the Flux documentation for further details. Note that, when the training data and parameters are simulated dynamically (i.e., \"on the fly\"; see On-the-fly and just-in-time simulation), overfitting is generally not a concern, making this form of regularisation unnecessary.\n\nFor illustration, the following code constructs a neural Bayes estimator using dropout and L₁ regularisation with penalty coefficient lambda = 10^-4:\n\nusing NeuralEstimators, Flux\n\n# Data Z|θ ~ N(θ, 1) with θ ~ N(0, 1)\nd = 1     # dimension of the parameter vector θ\nn = 1     # dimension of each independent replicate of Z\nm = 5     # number of independent replicates in each data set\nsampler(K) = randn32(d, K)\nsimulator(θ, m) = [μ .+ randn32(n, m) for μ ∈ eachcol(θ)]\nK = 3000  # number of training samples\nθ_train = sampler(K)\nθ_val   = sampler(K)\nZ_train = simulator(θ_train, m)\nZ_val   = simulator(θ_val, m)\n\n# Neural network with dropout layers\nw = 128\nψ = Chain(Dense(1, w, relu), Dropout(0.1), Dense(w, w, relu), Dropout(0.5))     \nϕ = Chain(Dense(w, w, relu), Dropout(0.5), Dense(w, 1))           \nnetwork = DeepSet(ψ, ϕ)\n\n# Initialise estimator\nestimator = PointEstimator(network)\n\n# Optimiser with L₁ regularisation\noptimiser = Flux.setup(OptimiserChain(SignDecay(1e-4), Adam()), estimator)\n\n# Train the estimator\ntrain(estimator, θ_train, θ_val, Z_train, Z_val; optimiser = optimiser)","category":"section"},{"location":"workflow/advancedusage/#Expert-summary-statistics","page":"Advanced usage","title":"Expert summary statistics","text":"Implicitly, neural estimators involve the learning of summary statistics. However, some summary statistics are available in closed form, simple to compute, and highly informative (e.g., sample quantiles, the empirical variogram). Often, explicitly incorporating these expert summary statistics in a neural estimator can simplify the optimisation problem, and lead to a better estimator.\n\nThe fusion of learned and expert summary statistics is facilitated by the DataSet struct, which couples raw data with a matrix of precomputed expert summary statistics. These are concatenated with the learned summary statistics during both training and inference, regardless of the chosen neural-network architecture. Note that a neural estimator based purely on expert summary statistics (see, e.g., Gerber and Nychka, 2021; Rai et al. ,2024) can be constructed by setting the summary network to identity and passing the summary statistics directly as a matrix. Note also that users may specify arbitrary expert summary statistics, however for convenience several standard User-defined summary statistics are provided with the package, including a fast, sparse approximation of the empirical variogram.\n\nFor an example of incorporating expert summary statistics, see Irregular spatial data, where the empirical variogram is used alongside learned graph-neural-network-based summary statistics.","category":"section"},{"location":"workflow/advancedusage/#Variable-sample-sizes","page":"Advanced usage","title":"Variable sample sizes","text":"A neural estimator based on the DeepSet representation can be applied to data sets of arbitrary sample size m. However, the posterior distribution, and summaries derived from it, typically depends on m, and neural estimators must account for this dependence if data sets with varying m are envisaged. \n\nnote: Aggregation functions and variable sample sizes\nWhen the sample size is m fixed, the choice of aggregation function used in the DeepSet representation is typically driven by considerations of numerical stability. For example, mean aggregation is often preferred over sum to avoid exploding gradients when m is large. However, not all functions that perform well for fixed sample sizes preserve the ability of the network to generalise across different sample sizes. Specifically, the mean aggregation function, while numerically stable, discards information about m. One has several options when designing a neural network to accomodate variable sample sizes. Use sum aggregation. This is effective with small to moderate values m, but can lead to exploding gradients when m becomes large. \nUse mean aggregation with m explicitly input to the neural network. This can be done by supplying logsamplesize or invsqrtsamplesize as an expert summary statistic when constructing the DeepSet. \nUse a hybrid aggregation that maintains numerical stability while implicitly encoding m. For example, weightedsum(X; dims) = logsamplesize(X) .* mean(X; dims). Since m is not explicitly provided to the network in this approach, it may be beneficial to combine it with the previous strategy. \n\nLet a data set consisting of m conditionally independent replicates be denoted by boldsymbolZ^(m) equiv (boldsymbolZ_1 dots boldsymbolZ_m). If data sets with varying m are envisaged, a generally applicable approach is to treat the sample size as a random variable, M, with support over a set of positive integers. One then places a distribution on M (e.g., discrete uniform) that is sampled from when simulating data during the training phase. Note that the choice of distribution for M is theoretically immaterial, provided it assigns positive mass on all sample sizes of interest (this follows from similar arguments given by Richards et al., 2024, Theorem 1 and Sainsbury-Dale et al., 2025, Theorem 1). \n\nTreating the sample size as random is an effective approach that only requires minor modifications to the workflow for fixed sample sizes. Specifically, the following pseudocode illustrates how one may modify a general data simulator to train under a range of sample sizes, with the distribution of M defined by passing any object that can be sampled using rand(M, K) (e.g., the integer range 1:30):\n\n# Method that allows M to be an object that can be sampled from\nfunction simulate(θ, M)\n\t# Number of parameter vectors \n\tK = size(θ, 2)\n\n\t# Generate K sample sizes from the distribution for M\n\tm = rand(M, K)\n\n\t# Pseudocode for data simulation\n\tZ = [<simulate m[k] realisations from the model given θ[:, k]> for k ∈ 1:K]\n\n\treturn Z\nend\n\n# Wrapper that allows a single fixed integer sample size\nsimulate(parameters, m::Integer) = simulate(parameters, range(m, m))\n\nThe following simple example illustrates this approach using a conjugate Gaussian model. Specifically, we assume a Gaussian likelihood with unknown mean mu, known variance sigma^2, and a Gaussian prior with mean mu_0 and variance sigma_0^2. \n\nusing NeuralEstimators, Flux\nusing DataFrames\nusing Distributions\nusing Plots\nusing StatsPlots\n\nn = 1  # dimension of each data replicate \nd = 1  # dimension of the parameter vector θ\nσ = 2  # known standard deviation of the data \nμ₀ = 1 # prior mean \nσ₀ = 2 # prior standard deviation \n\nfunction sample(K)\n    θ = rand(Normal(μ₀, σ₀), 1, K)\n    return θ\nend\n\nfunction simulate(θ, M)\n    Z = [ϑ .+  σ * randn(n, rand(M)) for ϑ ∈ eachcol(θ)]\n    return Z\nend\nsimulate(θ, m::Integer) = simulate(θ, range(m, m))\n\n# Approximate distribution based on dstar summary statistics \ndstar = 2d \nq = GaussianMixture(d, dstar)\n\n# Neural network mapping data to summary statistics of the same dimension used in q, \n# using default mean aggreation with log(m) as an \"expert summary statistic\"\nw = 128 \nS(Z) = log.(samplesize(Z))\nψ = Chain(\n\tDense(n, w, relu), \n\tDense(w, w, relu), \n\tDense(w, w, relu)\n\t)\nϕ = Chain(\n\tDense(w + 1, w, relu), # input dimension w + 1 to accommodate \"expert summary statistic\"\n\tDense(w, w, relu), \n\tDense(w, dstar)\n\t)\nnetwork = DeepSet(ψ, ϕ; S = S)\n\n# Initialise the neural posterior estimator\nestimator = PosteriorEstimator(q, network)\n\n# Train the estimator with the sample size randomly sampled between 2 and 25\nestimator = train(estimator, sample, simulate; K = 30_000, simulator_args = 2:25) \n\nSince the posterior is available in closed form for this example, we can directly compare the standard deviation of the neural posterior to the analytic posterior standard deviation as a function of the sample size m. Importantly, the analytic posterior standard deviation is deterministic; it depends only on sigma_0, sigma, and m, and not on the observed data. However, we do not expect a neural network to capture this behavior exactly, especially when trained on finite data. As such, some sampling variability in the estimated posterior standard deviation is expected, even when the true value is constant for a given m:\n\n# Closed-form expression for the posterior standard deviation\nanalyticstd(σ₀, σ, m) = sqrt(1 / (1 / σ₀^2 + m / σ^2))\n\n# Simulate data and return posterior standard deviation from estimator\nfunction simulate_neural_std(estimator, m)\n    μ = sample(1)\n    Z = simulate(μ, m)  \n    post_samples = sampleposterior(estimator, Z)\n    return std(post_samples, corrected = false)\nend\n\n# Sample sizes to evaluate\nm_range = [2, 10, 25]\n\n# Estimate posterior std dev for many data sets\nneural_std = map(m_range) do m \n    mapreduce(_ -> simulate_neural_std(estimator, m), vcat, 1:1000) \nend\ndf = DataFrame(\n\tneural_std = reduce(vcat, neural_std),\n\tm = repeat(m_range, inner = 1000)\n)\n\n# Boxplot: neural posterior std dev by sample size\n@df df boxplot(:m, :neural_std;\n    xlabel = \"Number of replicates (m)\",\n    ylabel = \"Neural posterior std. dev.\",\n    label = \"Neural posterior std. dev.\"\n)\n\n# Overlay analytic posterior std. dev.\ntrue_std = [analyticstd(σ₀, σ, m) for m in m_range]\nscatter!(m_range, true_std;\n    color = :red,\n    markershape = :circle,\n    label = \"Analytic posterior std. dev.\",\n    legend = :topright\n)\n\n(Image: Neural)","category":"section"},{"location":"workflow/advancedusage/#Missing-data","page":"Advanced usage","title":"Missing data","text":"Neural networks do not naturally handle missing data, and this property can preclude their use in a broad range of applications. Here, we describe two techniques that alleviate this challenge in the context of parameter point estimation: the masking approach and the expectation-maximisation (EM) approach. \n\nAs a running example, we consider a Gaussian process model where the data are collected over a regular grid, but where some elements of the grid are unobserved. This situation often arises in, for example, remote-sensing applications, where the presence of cloud cover prevents measurement in some places. Below, we load the packages needed in this example, and define some aspects of the model that will remain constant throughout (e.g., the prior, the spatial domain). We also define types and functions for sampling from the prior distribution and for simulating marginally from the data model.\n\nusing NeuralEstimators, Flux\nusing Distributions: Uniform\nusing Distances, LinearAlgebra\nusing Statistics: mean\n\n# Prior and dimension of parameter vector\nΠ = (τ = Uniform(0, 1.0), ρ = Uniform(0, 0.4))\nd = length(Π)\n\n# Define the grid and compute the distance matrix\npoints = range(0, 1, 16)\nS = expandgrid(points, points)\nD = pairwise(Euclidean(), S, dims = 1)\n\n# Collect model information for later use\nξ = (Π = Π, S = S, D = D)\n\n# Struct for storing parameters and Cholesky factors\nstruct Parameters <: AbstractParameterSet\n\tθ\n\tL\nend\n\n# Constructor for above struct\nfunction sample(K::Integer, ξ)\n\n\t# Sample parameters from the prior\n\tΠ = ξ.Π\n\tτ = rand(Π.τ, K)\n\tρ = rand(Π.ρ, K)\n\tν = 1 # fixed smoothness\n\n\t# Compute Cholesky factors  \n\tL = maternchols(ξ.D, ρ, ν)\n\n\t# Concatenate into matrix\n\tθ = permutedims(hcat(τ, ρ))\n\n\tParameters(θ, L)\nend\n\n# Marginal simulation from the data model\nfunction simulate(parameters::Parameters, m::Integer)\n\n\tK = size(parameters, 2)\n\tτ = parameters.θ[1, :]\n\tL = parameters.L\n\tG = isqrt(size(L, 1)) # side-length of grid\n\n\tZ = map(1:K) do k\n\t\tz = simulategaussian(L[:, :, k], m)\n\t\tz = z + τ[k] * randn(size(z)...)\n\t\tz = reshape(z, G, G, 1, :)\n\t\tz\n\tend\n\n\treturn Z\nend","category":"section"},{"location":"workflow/advancedusage/#The-masking-approach","page":"Advanced usage","title":"The masking approach","text":"The first missing-data technique that we consider is the so-called masking approach of Wang et al. (2024); see also the discussion by Sainsbury-Dale et al. (2025, Sec. 2.2). The strategy involves completing the data by replacing missing values with zeros, and using auxiliary variables to encode the missingness pattern, which are also passed into the network.\n\nLet boldsymbolZ denote the complete-data vector. Then, the masking approach considers inference based on boldsymbolW, a vector of indicator variables that encode the missingness pattern (with elements equal to one or zero if the corresponding element of boldsymbolZ is observed or missing, respectively), and\n\nboldsymbolU equiv boldsymbolZ odot boldsymbolW\n\nwhere odot denotes elementwise multiplication and the product of a missing element and zero is defined to be zero. Irrespective of the missingness pattern, boldsymbolU and boldsymbolW have the same fixed dimensions and hence may be processed easily using a single neural network. A neural estimator is then trained on realisations of boldsymbolU boldsymbolW which, by construction, do not contain any missing elements. \n\nThe manner in which boldsymbolU and boldsymbolW are combined depends on the multivariate structure of the data and the chosen architecture. For example, when the data are gridded and the neural network is a CNN, then boldsymbolU and boldsymbolW can be concatenated along the channels dimension (i.e., the penultimate dimension of the array).  The construction of augmented data sets boldsymbolU boldsymbolW from incomplete data is facilitated by the helper function encodedata().\n\nSince the missingness pattern boldsymbolW is now an input to the neural network, it must be incorporated during the training phase. When interest lies only in making inference from a single already-observed data set, boldsymbolW is fixed and known, and the Bayes risk remains unchanged. However, amortised inference, whereby one trains a single neural network that will be used to make inference with many data sets, requires a joint model for the data boldsymbolZ and the missingness pattern boldsymbolW, which is here defined as follows (see the helper function removedata()):\n\n# Marginal simulation from the data model and a MCAR missingness model\nfunction simulatemissing(parameters::Parameters, m::Integer)\n\n\tZ = simulate(parameters, m)   # complete data\n\n\tUW = map(Z) do z\n\t\tprop = rand()             # sample a missingness proportion\n\t\tz = removedata(z, prop)   # randomly remove a proportion of the data\n\t\tuw = encodedata(z)        # replace missing entries with zero and encode missingness pattern\n\t\tuw\n\tend\n\n\treturn UW\nend\n\nNext, we construct and train a masked neural Bayes estimator using a CNN architecture. Here, the first convolutional layer takes two input channels, since we store the augmented data boldsymbolU in the first channel and the missingness pattern boldsymbolW in the second. We construct a point estimator, but the masking approach is applicable with any other kind of estimator (see Estimators):\n\n# Construct DeepSet object\nψ = Chain(\n\tConv((10, 10), 2 => 16,  relu),\n\tConv((5, 5),  16 => 32,  relu),\n\tConv((3, 3),  32 => 64, relu),\n\tFlux.flatten\n\t)\nϕ = Chain(Dense(64, 256, relu), Dense(256, d, exp))\nnetwork = DeepSet(ψ, ϕ)\n\n# Initialise point estimator\nθ̂ = PointEstimator(network)\n\n# Train the masked neural Bayes estimator\nθ̂ = train(θ̂, sample, simulatemissing, simulator_args = 1, ξ = ξ, K = 1000, epochs = 10)\n\nOnce trained, we can apply our masked neural Bayes estimator to (incomplete) observed data. The data must be encoded in the same manner as during training. Below, we use simulated data as a surrogate for real data, with a missingness proportion of 0.25:\n\nθ = sample(1, ξ)     # true parameters\nZ = simulate(θ, 1)[1]    # complete data\nZ = removedata(Z, 0.25)  # \"observed\" incomplete data (i.e., with missing values)\nUW = encodedata(Z)       # augmented data {U, W}\nθ̂(UW)                    # point estimate","category":"section"},{"location":"workflow/advancedusage/#The-EM-approach","page":"Advanced usage","title":"The EM approach","text":"Let boldsymbolZ_1 and boldsymbolZ_2 denote the observed and unobserved (i.e., missing) data, respectively, and let boldsymbolZ equiv (boldsymbolZ_1 boldsymbolZ_2) denote the complete data. A classical approach to facilitating inference when data are missing is the expectation-maximisation (EM) algorithm. The neural EM algorithm (Sainsbury-Dale et al., 2025, Sec. 2.3) is an approximate version of the conventional (Bayesian) Monte Carlo EM algorithm which, at the lth iteration, updates the parameter vector through\n\nboldsymboltheta^(l) = undersetboldsymbolthetamathrmargmax sum_h = 1^H ell(boldsymboltheta  boldsymbolZ_1  boldsymbolZ_2^(l h)) + log pi_H(boldsymboltheta)\n\nwhere realisations of the missing-data component, boldsymbolZ_2^(l h)  h = 1 dots H, are sampled from the probability distribution of boldsymbolZ_2 given boldsymbolZ_1 and boldsymboltheta^(l-1), and where pi_H(boldsymboltheta) propto pi(boldsymboltheta)^H is a concentrated version of the original prior density. Given the conditionally simulated data, the neural EM algorithm performs the above EM update using a neural network that returns the MAP estimate (i.e., the posterior mode) using (complete) conditionally simulated data. \n\nFirst, we construct a neural approximation of the MAP estimator. In this example, we will take H=50. When H is taken to be reasonably large and the prior is uniform, one may lean on the Bernstein-von Mises theorem to train the neural Bayes estimator under linear or quadratic loss; otherwise, one should train the estimator under a continuous relaxation of the 0–1 loss (e.g., the tanhloss() in the limit kappa to 0). This is done as follows:\n\n# Construct DeepSet object\nψ = Chain(\n\tConv((10, 10), 1 => 16,  relu),\n\tConv((5, 5),  16 => 32,  relu),\n\tConv((3, 3),  32 => 64, relu),\n\tFlux.flatten\n\t)\nϕ = Chain(\n\tDense(64, 256, relu),\n\tDense(256, d, exp)\n\t)\nnetwork = DeepSet(ψ, ϕ)\n\n# Initialise point estimator\nθ̂ = PointEstimator(network)\n\n# Train neural Bayes estimator\nH = 50\nθ̂ = train(θ̂, sample, simulate, simulator_args = H, ξ = ξ, K = 1000, epochs = 10)\n\nNext, we define a function for conditional simulation (see EM for details on the required format of this function):\n\nfunction simulateconditional(Z::M, θ; nsims::Integer = 1, ξ) where {M <: AbstractMatrix{Union{Missing, T}}} where T\n\n\t# Save the original dimensions\n\tdims = size(Z)\n\n\t# Convert to vector\n\tZ = vec(Z)\n\n\t# Compute the indices of the observed and missing data\n\tI₁ = findall(z -> !ismissing(z), Z) # indices of observed data\n\tI₂ = findall(z -> ismissing(z), Z)  # indices of missing data\n\tn₁ = length(I₁)\n\tn₂ = length(I₂)\n\n\t# Extract the observed data and drop Missing from the eltype of the container\n\tZ₁ = Z[I₁]\n\tZ₁ = [Z₁...]\n\n\t# Distance matrices needed for covariance matrices\n\tD   = ξ.D # distance matrix for all locations in the grid\n\tD₂₂ = D[I₂, I₂]\n\tD₁₁ = D[I₁, I₁]\n\tD₁₂ = D[I₁, I₂]\n\n\t# Extract the parameters from θ\n\tτ = θ[1]\n\tρ = θ[2]\n\n\t# Compute covariance matrices\n\tν = 1 # fixed smoothness\n\tΣ₂₂ = matern.(UpperTriangular(D₂₂), ρ, ν); Σ₂₂[diagind(Σ₂₂)] .+= τ^2\n\tΣ₁₁ = matern.(UpperTriangular(D₁₁), ρ, ν); Σ₁₁[diagind(Σ₁₁)] .+= τ^2\n\tΣ₁₂ = matern.(D₁₂, ρ, ν)\n\n\t# Compute the Cholesky factor of Σ₁₁ and solve the lower triangular system\n\tL₁₁ = cholesky(Symmetric(Σ₁₁)).L\n\tx = L₁₁ \\ Σ₁₂\n\n\t# Conditional covariance matrix, cov(Z₂ ∣ Z₁, θ),  and its Cholesky factor\n\tΣ = Σ₂₂ - x'x\n\tL = cholesky(Symmetric(Σ)).L\n\n\t# Conditonal mean, E(Z₂ ∣ Z₁, θ)\n\ty = L₁₁ \\ Z₁\n\tμ = x'y\n\n\t# Simulate from the distribution Z₂ ∣ Z₁, θ ∼ N(μ, Σ)\n\tz = randn(n₂, nsims)\n\tZ₂ = μ .+ L * z\n\n\t# Combine the observed and missing data to form the complete data\n\tZ = map(1:nsims) do l\n\t\tz = Vector{T}(undef, n₁ + n₂)\n\t\tz[I₁] = Z₁\n\t\tz[I₂] = Z₂[:, l]\n\t\tz\n\tend\n\tZ = stackarrays(Z, merge = false)\n\n\t# Convert Z to an array with appropriate dimensions\n\tZ = reshape(Z, dims..., 1, nsims)\n\n\treturn Z\nend\n\nNow we can use the neural EM algorithm to get parameter point estimates from data containing missing values. The algorithm is implemented with the type EM. Again, here we use simulated data as a surrogate for real data:\n\nθ = sample(1, ξ)            # true parameters\nZ = simulate(θ, 1)[1][:, :]     # complete data\nZ = removedata(Z, 0.25)         # \"observed\" incomplete data (i.e., with missing values)\nθ₀ = mean.([Π...])              # initial estimate, the prior mean\n\nneuralem = EM(simulateconditional, θ̂)\nneuralem(Z, θ₀, nsims = H, ξ = ξ)","category":"section"},{"location":"workflow/advancedusage/#Censored-data","page":"Advanced usage","title":"Censored data","text":"Neural estimators can be constructed to handle censored data as input, by exploiting the masking approach described above in the context of missing data. For simplicity, here we describe inference with left censored data (i.e., where we observe only those data that exceed some threshold), but extensions to right or interval censoring are possible. We first present the framework for General censoring, where data are considered censored based on an arbitrary, user-defined censoring scheme. We then consider Peaks-over-threshold censoring, a special case in which the data are treated as censored if they do not exceed their corresponding marginal tau-quantile for tau in (01) close to one (Richards et al., 2024).\n\nAs a running example, we consider a bivariate random scale Gaussian mixture copula; see Engelke, Opitz, and Wadsworth (2019) and Huser and Wadsworth (2019). We consider the task of estimating boldsymboltheta=(rhodelta), for correlation parameter rho in (-11) and shape parameter delta in 01. Variables boldsymbolY_1dotsboldsymbolY_m are independent and identically distributed according to the random scale construction\n\nboldsymbolY_i = delta R_i + (1-delta)  boldsymbolX_i\n\nwhere R_i sim textExp(1) and boldsymbolX_i is a bivariate random vector following a Gaussian copula with correlation rho and unit exponential margins. We note that the vector boldsymbolY_i does not itself have unit exponential margins. Instead, its marginal distribution function, F(ydelta) is dependent on delta; this has a closed form expression, see Huser and Wadsworth (2019). In practice, the parameter delta is unknown, and so the random scale construction is treated as a copula and fitted to standardised uniform data. That is, the data used for inference are boldsymbolZ_i = F(boldsymbolY_i delta) which have been transformed to a uniform scale via the delta-dependent marginal dsitribution function.   \n\nSimulation of the random scale mixture (on uniform margins) and its marginal ditribution function are provided below. Transforming the data to exponential margins can, in some cases, enhance training efficiency (Richards et al., 2024). However, for simplicity, we do not apply this transformation here.\n\n# Libraries used throughout this example\nusing NeuralEstimators, Flux\nusing Folds\nusing CUDA # GPU if it is available\nusing LinearAlgebra: Symmetric, cholesky\nusing Distributions: cdf, Uniform, Normal, quantile\nusing CairoMakie   \n\n# Sampling θ from the prior distribution\nfunction sample(K)\n\tρ = rand(Uniform(-0.99, 0.99), K)\n\tδ = rand(Uniform(0.0, 1.0), K)\n\tθ = vcat(ρ', δ')\n\treturn θ \nend\n\n# Marginal simulation of Z | θ\nfunction simulate(θ, m) \n\tZ = Folds.map(1:size(θ, 2)) do k\n\t\tρ = θ[1, k]\n\t\tδ = θ[2, k]\n\t\tΣ = [1 ρ; ρ 1]\n\t\tL = cholesky(Symmetric(Σ)).L\n\t\tX = L * randn(2, m)                 # Standard Gaussian margins\n\t\tX = -log.(1 .- cdf.(Normal(), X))   # Transform to unit exponential margins\n\t\tR = -log.(1 .- rand(1, m))         \n\t\tY = δ .* R .+ (1 - δ) .* X          \n\t\tZ = F.(Y; δ = δ)                     # Transform to uniform margins\n\tend\n\treturn Z\nend\n\n# Marginal distribution function; see Huser and Wadsworth (2019)\nfunction F(y; δ)\n\tif δ == 0.5 \n        u = 1 .- exp.(- 2 .* y) .* (1 .+ 2 .* y) \n    else \n        u = 1 .- (δ ./ (2 .* δ .- 1)) .* exp.(- y ./ δ) .+ ((1 .- δ) ./ (2 * δ .- 1)) .* exp.( - y ./ (1 - δ)) \n    end\n\treturn u\nend","category":"section"},{"location":"workflow/advancedusage/#General-censoring","page":"Advanced usage","title":"General censoring","text":"Inference with censored data can proceed in an analogous manner to the The masking approach for missing data. First, consider a vector boldsymbolW of indicator variables that encode the censoring pattern, with elements equal to one or zero if the corresponding element of the data boldsymbolZ equiv (Z_1 dots Z_n) is censored or observed, respectively. That is, boldsymbolW equiv (mathbbI(Z_j  leq c_j)  j = 1 dots n) where c_j, j = 1 dots n, is a censoring threshold. Second, consider an augmented data vector \n\nboldsymbolU equiv boldsymbolZ odot boldsymbolW + boldsymbolv odot ( boldsymbol1 - boldsymbolW)\n\nwhere boldsymbol1 is a vector of ones of appropriate dimension, boldsymbolv in mathbbR^n is user-defined, and odot denotes elementwise multiplication. A neural estimator for censored data is then trained on realisations of the augmented data set, boldsymbolU boldsymbolW. \n\nThe manner in which boldsymbolU and boldsymbolW are combined depends on the multivariate structure of the data and the chosen architecture. For example, when the data are gridded and the neural network is a CNN, then boldsymbolU and boldsymbolW can be concatenated along the channels dimension (i.e., the penultimate dimension of the array). In this example, we have replicated, unstructured bivariate data stored as matrices of dimension 2times m, where m denotes the number of independent replicates, and so the neural network is based on dense multilayer perceptrons (MLPs). In these settings, a simple way to combine boldsymbolU and boldsymbolW so that they can be passed through the neural network is to concatenate boldsymbolU and boldsymbolW along their first dimension, so that the resulting input is a matrix of dimension 4 times m. \n\nThe following helper function implements a simple version of the general censoring framework described above, based on a vector of censoring levels boldsymbolc and with boldsymbolv fixed to a constant such that the censoring mechanism and augmentation values do not vary with the model parameter values or with the replicate index.\n\n# Constructing augmented data from Z and the censoring threshold c\nfunction censorandaugment(Z; c, v = -1.0)\n    W = 1 * (Z .<= c)\n    U = ifelse.(Z .<= c, v, Z)\n    return vcat(U, W)\nend\n\nThe above censoring function can then be incorporated into the data simulator as follows. \n\n# Marginal simulation of censored data\nfunction simulatecensored(θ, m; kwargs...) \n\tZ = simulate(θ, m)\n\tUW = Folds.map(Z) do Zₖ\n\t\tmapslices(Z -> censorandaugment(Z; kwargs...), Zₖ, dims = 1)\n\tend\nend\n\nBelow, we construct a neural point estimator for censored data, based on a DeepSet architecture.\n\nn = 2    # dimension of each data replicate (bivariate)\nw = 128  # width of each hidden layer\n\n# Final layer has output dimension d=2 and enforces parameter constraints\nfinal_layer = Parallel(\n    vcat,\n    Dense(w, 1, tanh),    # ρ ∈ (-1,1)\n    Dense(w, 1, sigmoid)  # δ ∈ (0,1)\n)\nψ = Chain(Dense(n * 2, w, relu), Dense(w, w, relu))    \nϕ = Chain(Dense(w, w, relu), final_layer)           \nnetwork = DeepSet(ψ, ϕ)\n\n# Initialise the estimator\nestimator = PointEstimator(network)\n\nWe now train and assess two estimators for censored data; one with c = [0, 0] and one with c = [0.5, 0.5]. When the data boldsymbolZ are on uniform margins, the components of c can be interpreted as the expected number of censored values in each component; thus, c = [0, 0] corresonds to no censoring of the data, and c = [0.5, 0.5] corresponds to a situation where, on average, 50% of each dimension Z_j is censored. As expected, the neural estimator that uses non-censored data has lower RMSE, as the data it uses contain more information.\n\n# Number of independent replicates in each data set\nm = 200 \n\n# Train an estimator with no censoring\nsimulator1(θ, m) = simulatecensored(θ, m; c = [0, 0]) \nestimator1 = train(estimator, sample, simulator1, simulator_args = m) \n\n# Train an estimator with mild censoring\nsimulator2(θ, m) = simulatecensored(θ, m; c = [0.5, 0.5]) \nestimator2 = train(estimator, sample, simulator2, simulator_args = m)\n\n# Assessment\nθ_test = sample(1000) \nUW_test1 = simulator1(θ_test, m)\nUW_test2 = simulator2(θ_test, m)\nassessment1 = assess(estimator1, θ_test, UW_test1, parameter_names = [\"ρ\", \"δ\"], estimator_name = \"No censoring\") \nassessment2 = assess(estimator2, θ_test, UW_test2, parameter_names = [\"ρ\", \"δ\"], estimator_name = \"Mild censoring\")   \nassessment  = merge(assessment1, assessment2)\nrmse(assessment)\nplot(assessment)\n\nEstimator Parameter RMSE\nNo censoring ρ 0.238167\nNo censoring δ 0.100688\nMild censoring ρ 0.394838\nMild censoring δ 0.135169\n\n(Image: General censoring)\n\nHere we have trained two separate neural estimators to handle two different censoring threshold vectors. However, one could train a single neural estimator that caters for a range of censoring thresholds, c, by allowing it to vary with the data samples and using it as an input to the neural network. In the next section, we illustrate this in the context of peaks-over-threshold modelling, whereby a single censoring threshold is defined to be the marginal tau-quantile of the data, and we amortise the estimator with respect to the probability level tau. In a peaks-over-threshold setting, variation in the censoring thresholds can be created by placing a prior on tau, which induces a prior on c.","category":"section"},{"location":"workflow/advancedusage/#Peaks-over-threshold-censoring","page":"Advanced usage","title":"Peaks-over-threshold censoring","text":"Richards et al. (2024) discuss neural Bayes estimation from censored data in the context of peaks-over-threshold extremal dependence modelling, where deliberate censoring of data is imposed to reduce estimation bias in the presence of marginally non-extreme events. In these settings, data are treated as censored if they do not exceed their corresponding marginal tau-quantile, for tau in (01) close to one. \n\nPeaks-over-threshold censoring, with tau fixed, can be easily implemented using the General censoring framework by setting the censoring threshold equal to the tau-th quantile of the data boldsymbolZ. Further, Richards et al. (2024) show that one can amortise a neural estimator with respect to the choice of tau by treating it as an input to the neural network. \n\nBelow, we sample a fixed set of K parameter-data pairs for training the neural network. \n\n# Sampling values of τ to use during training \nfunction sampleτ(K)\n\tτ = rand(Uniform(0.0, 0.9), 1, K) \nend\n\n# Adapt the censored data simulation to allow for τ as an input\nfunction simulatecensored(θ, τ, m; kwargs...) \n    Z = simulate(θ, m)\n\tK = size(θ, 2)\n\tUW = Folds.map(1:K) do k\n        Zₖ = Z[k]\n        τₖ = τ[k]\n        cₖ = τₖ # data are on uniform margins: censoring threshold equals τ\n        mapslices(Z -> censorandaugment(Z; c = cₖ, kwargs...), Zₖ, dims = 1)\n\tend\nend\n\n# Generate the data used for training and validation\nK = 50000  # number of training samples\nm = 500    # number of independent replicates in each data set\nθ_train  = sample(K)\nθ_val    = sample(K ÷ 5)\nτ_train  = sampleτ(K)\nτ_val    = sampleτ(K ÷ 5)\nUW_train = simulatecensored(θ_train, τ_train, m)\nUW_val   = simulatecensored(θ_val, τ_val, m)\n\nIn this example, the probability level tau can be incorporated as an input to the neural network by treating it as an input to the outer neural network of the DeepSet architecture. To do this, we increase the input dimension of the outer network by one, and then combine the data boldsymbolU boldsymbolW and tau as a tuple (see DeepSet for details). \n\n# Construct neural network based on DeepSet architecture\nψ = Chain(Dense(n * 2, w, relu),Dense(w, w, relu))    \nϕ = Chain(Dense(w + 1, w, relu), final_layer)\nnetwork = DeepSet(ψ, ϕ)\n\n# Initialise the estimator\nestimator = PointEstimator(network)\n\n# Train the estimator\nestimator = train(estimator, θ_train, θ_val, (UW_train, τ_train), (UW_val, τ_val))\n\nOur trained estimator can now be used for any value of tau within the range used during training (tau in 009). Since the estimator is amortised with respect to tau, there is no need to retrain it for different degrees of censoring.\n\nBelow, we assess the estimator for different values of tau. As expected, RMSE increases with tau (larger tau corresponds to more censoring). \n\n# Test parameters\nθ_test = sample(1000)\n\n# Assessment with τ fixed to 0 (no censoring)\nτ_test1  = fill(0.0, 1000)'\nUW_test1 = simulatecensored(θ_test, τ_test1, m)\nassessment1 = assess(estimator, θ_test, (UW_test1, τ_test1), parameter_names = [\"ρ\", \"δ\"], estimator_name = \"τ = 0\")   \n\n# Assessment with τ fixed to 0.8\nτ_test2  = fill(0.8, 1000)'\nUW_test2 = simulatecensored(θ_test, τ_test2, m)\nassessment2 = assess(estimator, θ_test, (UW_test2, τ_test2), parameter_names = [\"ρ\", \"δ\"], estimator_name = \"τ = 0.8\")   \n\n# Compare results between the two censoring probability levels\nassessment = merge(assessment1, assessment2)\nrmse(assessment)\nplot(assessment)\n\nEstimator Parameter RMSE\nτ = 0 ρ 0.241780\nτ = 0 δ 0.096779\nτ = 0.80 ρ 0.476348\nτ = 0.80 δ 0.124913\n\n(Image: Peaks-over-threshold censoring)","category":"section"},{"location":"workflow/overview/#Overview","page":"Overview","title":"Overview","text":"Neural inferential methods have marked practical appeal, as their implementation is only loosely connected to the statistical or physical model being considered. The workflow when using the package NeuralEstimators is as follows:\n\nSample parameters from the prior, pi(boldsymboltheta), to form training/validation/test parameter sets. Alternatively, define a function to sample parameters dynamically during training. Parameters are stored as d times K matrices, with d the dimensionality of the parameter vector and K the number of parameter vectors in the given parameter set. \nSimulate data from the model conditional on the above parameter sets, to form training/validation/test data sets. Alternatively, define a function to simulate data dynamically during training. Simulated data sets are stored as mini-batches in a format amenable to the chosen neural-network architecture (see Step 3). \nDesign and initialise a suitable neural network that maps data to mathbbR^d^* (i.e., learned summary statistics of user-specified dimension d^*). The architecture class (e.g., MLP, CNN, GNN, DeepSet) should reflect structure of the data (e.g., unstructured, grid, graph, replicated), and any Flux model can be used. Given K data sets stored appropriately (see Step 2), the neural network should output a d^* times K matrix. \nConstruct a NeuralEstimator by wrapping the neural network in the subtype corresponding to the intended inferential method:\nPointEstimator: neural Bayes estimators under user-defined loss functions;\nPosteriorEstimator: neural posterior estimators (also choose an  approximate distribution);\nRatioEstimator: neural ratio estimators.\nTrain the NeuralEstimator using train() and the training set, monitoring performance and convergence using the validation set. For generic neural Bayes estimators, specify a loss function. \nAssess the NeuralEstimator using assess() and the test set. \n\nOnce the NeuralEstimator has passed our assessments and is deemed to be well calibrated, it may be used to make inference with observed data. \n\nNext, see the Examples and, once familiar with the basic workflow, see Advanced usage for further practical considerations on how to most effectively construct neural estimators.","category":"section"},{"location":"API/core/#Core","page":"Core","title":"Core","text":"This page documents the classes and functions that are central to the workflow of NeuralEstimators. Its organisation reflects the order in which these classes and functions appear in a standard implementation: from sampling parameters from the prior distribution, to making inference with observed data.","category":"section"},{"location":"API/core/#Sampling-parameters","page":"Core","title":"Sampling parameters","text":"Parameters sampled from the prior distribution are stored as a d times K matrix, where d is the dimension of the parameter vector to make inference on and K is the number of sampled parameter vectors. \n\nIt can sometimes be helpful to wrap the parameter matrix in a user-defined type that also stores expensive intermediate objects needed for data simulated (e.g., Cholesky factors). The user-defined type should be a subtype of AbstractParameterSet, whose only requirement is a field θ that stores the matrix of parameters. See Storing expensive intermediate objects for data simulation for further discussion.   ","category":"section"},{"location":"API/core/#Simulating-data","page":"Core","title":"Simulating data","text":"The package accommodates any model for which simulation is feasible by allowing users to define their model implicitly through simulated data. \n\nSimulated data sets are stored as mini-batches in a format amenable to the chosen neural-network architecture. For example, when constructing an estimator from data collected over a grid, one may use a generic CNN, with each data set stored in the final dimension of a four-dimensional array. When performing inference from replicated data, a DeepSet architecture may be used, where simulated data sets are stored in a vector, and conditionally independent replicates are stored as mini-batches within each element of the vector.\n\nExpert summary statistics can be incorporated by wrapping the simulated data in a DataSet object, which couples the raw data with a matrix of precomputed summary statistics.","category":"section"},{"location":"API/core/#Estimators","page":"Core","title":"Estimators","text":"The package provides several classes of neural estimators, organised within a type hierarchy. At the top-level of the hierarchy is NeuralEstimator, an abstract supertype for all neural estimators in the package. \n\nNeural Bayes estimators are implemented as subtypes of the abstract supertype BayesEstimator. The simple type PointEstimator is used for constructing neural Bayes estimators under general, user-defined loss functions. Several specialised types cater for the estimation of posterior quantiles based on the quantile loss function: see IntervalEstimator and its generalisation QuantileEstimator for estimating posterior quantiles for a fixed set of probability levels. \n\nThe type PosteriorEstimator can be used to approximate the posterior distribution, and RatioEstimator can be used to approximate the likelihood-to-evidence ratio.\n\nFinally, Ensemble combines multiple estimators, aggregating their individual estimates to improve accuracy.","category":"section"},{"location":"API/core/#Helper-functions","page":"Core","title":"Helper functions","text":"The following helper functions operate on an estimator to inspect its components or apply parts of it to data. For the main inference functions used post-training, see Inference with observed data.","category":"section"},{"location":"API/core/#Training","page":"Core","title":"Training","text":"The function train() is used to train a neural estimator.\n\nAfter training, the risk history and optimiser state can be accessed and inspected using loadrisk(), plotrisk(), and loadoptimiser().","category":"section"},{"location":"API/core/#Assessment/calibration","page":"Core","title":"Assessment/calibration","text":"","category":"section"},{"location":"API/core/#Inference-with-observed-data","page":"Core","title":"Inference with observed data","text":"The following functions facilitate the use of a trained neural estimator with observed data. ","category":"section"},{"location":"API/core/#NeuralEstimators.AbstractParameterSet","page":"Core","title":"NeuralEstimators.AbstractParameterSet","text":"AbstractParameterSet\n\nAn abstract supertype for user-defined types that store parameters and any intermediate objects needed for data simulation.\n\nThe user-defined type must have a field θ that stores the d × K matrix of parameters, where d is the dimension of the parameter vector to make  inference on and K is the number of sampled parameter vectors. There are no other requirements.\n\nSee subsetparameters() for the generic function for subsetting these objects. \n\nExamples\n\nstruct P <: AbstractParameterSet\n\tθ\n\t# other expensive intermediate objects...\nend\n\n\n\n\n\n","category":"type"},{"location":"API/core/#NeuralEstimators.DataSet","page":"Core","title":"NeuralEstimators.DataSet","text":"DataSet(Z, S)\nDataSet(Z)\n\nA container that couples raw data Z with precomputed expert summary statistics S (a matrix with one column per data set). Passing a DataSet to any neural estimator causes the summary network to be applied to Z, with the resulting learned summary statistics concatenated with S before being passed to the inference network:\n\nboldsymbolt(mathbfZ) = (textsummary_network(mathbfZ) mathbfS)\n\nSince S is precomputed and stored as a plain matrix, no special treatment is needed during training: gradients do not flow through S.\n\nIf S is not provided, DataSet(Z) is equivalent to passing Z directly.\n\nSee also summarystatistics.\n\nExamples\n\nusing NeuralEstimators\nusing Statistics: mean, var\n\n# Simulate data: Z|μ,σ ~ N(μ, σ²)\nn, m, K = 1, 50, 500\nθ = rand(2, K)\nZ = [θ[1, k] .+ θ[2, k] .* randn(n, m) for k in 1:K]\n\n# Precompute expert summary statistics (e.g., sample mean and variance)\nS = hcat([vcat(mean(z), var(z)) for z in Z]...)\n\n# Package into a DataSet object\ndatasets = DataSet(Z, S)\n\n\n\n\n\n","category":"type"},{"location":"API/core/#NeuralEstimators.NeuralEstimator","page":"Core","title":"NeuralEstimators.NeuralEstimator","text":"NeuralEstimator\n\nAn abstract supertype for all neural estimators.\n\n\n\n\n\n","category":"type"},{"location":"API/core/#NeuralEstimators.BayesEstimator","page":"Core","title":"NeuralEstimators.BayesEstimator","text":"BayesEstimator <: NeuralEstimator\n\nAn abstract supertype for neural Bayes estimators.\n\n\n\n\n\n","category":"type"},{"location":"API/core/#NeuralEstimators.PointEstimator","page":"Core","title":"NeuralEstimators.PointEstimator","text":"PointEstimator <: BayesEstimator\nPointEstimator(network)\nPointEstimator(summary_network, inference_network)\nPointEstimator(summary_network, num_parameters; num_summaries, kwargs...)\n\nA neural point estimator, where the neural network is a mapping from the sample space to the parameter space.\n\nExamples\n\nusing NeuralEstimators, Flux, CairoMakie\n\n# Data Z|μ,σ ~ N(μ, σ²) with priors μ ~ U(0, 1) and σ ~ U(0, 1)\nnum_parameters = 2     # dimension of the parameter vector θ\nn = 1                  # dimension of each independent replicate of Z\nm = 50                 # number of independent replicates in each data set\nsampler(K) = rand32(num_parameters, K)\nsimulator(θ, m) = [ϑ[1] .+ ϑ[2] .* randn32(n, m) for ϑ in eachcol(θ)]\n\n# Neural network\nw = 128   \nψ = Chain(Dense(n, w, relu), Dense(w, w, relu), Dense(w, w, relu))\nϕ = Chain(Dense(w, w, relu), Dense(w, w, relu), Dense(w, num_parameters))\nnetwork = DeepSet(ψ, ϕ)\n\n# Initialise the estimator\nestimator = PointEstimator(network)\n\n# Train the estimator\nestimator = train(estimator, sampler, simulator, simulator_args = m, K = 1000)\n\n# Plot the risk history\nplotrisk()\n\n# Assess the estimator\nθ_test = sampler(500)\nZ_test = simulator(θ_test, m);\nassessment = assess(estimator, θ_test, Z_test)\nplot(assessment)\n\n# Inference with \"observed\" data \nθ = [0.8f0 0.1f0]'\nZ = simulator(θ, m)\nestimate(estimator, Z)\n\n\n\n\n\n","category":"type"},{"location":"API/core/#NeuralEstimators.PosteriorEstimator","page":"Core","title":"NeuralEstimators.PosteriorEstimator","text":"PosteriorEstimator <: NeuralEstimator\nPosteriorEstimator(summary_network, q::ApproximateDistribution)\nPosteriorEstimator(summary_network, num_parameters::Integer; num_summaries::Integer, q = NormalisingFlow, kwargs...)\n\nA neural estimator that approximates the posterior distribution p(boldsymboltheta mid boldsymbolZ), based on a neural summary_network and an approximate distribution q (see the available in-built Approximate distributions).\n\nThe summary_network maps data boldsymbolZ to a vector of learned summary statistics boldsymbolt in mathbbR^d^*, which are then used to condition the approximate distribution q. The precise way in which the summary statistics condition q depends on the choice of approximate distribution: for example, GaussianMixture uses an MLP to map boldsymbolt directly to distributional parameters, while NormalisingFlow uses boldsymbolt as a conditioning input at each coupling layer.\n\nThe convenience constructor builds q internally given num_parameters and num_summaries, with any additional keyword arguments passed to the constructor of q.\n\nKeyword arguments\n\nnum_summaries::Integer: the number of summary statistics output by summary_network. Must match the output dimension of summary_network.\nq::Type{<:ApproximateDistribution} = NormalisingFlow: the type of approximate distribution to use.\nkwargs...: additional keyword arguments passed to the constructor of q.\n\nExamples\n\nusing NeuralEstimators, Flux, CairoMakie\n\n# Data Z|μ,σ ~ N(μ, σ²) with priors μ ~ U(0, 1) and σ ~ U(0, 1)\nnum_parameters = 2  # dimension of the parameter vector θ\nn = 1               # dimension of each independent replicate of Z\nm = 50              # number of independent replicates in each data set\nsampler(K) = rand32(num_parameters, K)\nsimulator(θ, m) = [ϑ[1] .+ ϑ[2] .* randn32(n, m) for ϑ in eachcol(θ)]\n\n# Summary network \nnum_summaries = 4num_parameters\nw = 128   \nψ = Chain(Dense(n, w, relu), Dense(w, w, relu), Dense(w, w, relu))\nϕ = Chain(Dense(w, w, relu), Dense(w, w, relu), Dense(w, num_summaries))\nsummary_network = DeepSet(ψ, ϕ)\n\n# Initialise the estimator, with q built internally\nestimator = PosteriorEstimator(summary_network, num_parameters; num_summaries = num_summaries)\n\n# Or, build q explicitly\nq = NormalisingFlow(num_parameters; num_summaries = num_summaries)\nestimator = PosteriorEstimator(summary_network, q)\n\n# Train the estimator\nestimator = train(estimator, sampler, simulator, simulator_args = m, K = 3000)\n\n# Plot the risk history\nplotrisk()\n\n# Assess the estimator\nθ_test = sampler(500)\nZ_test = simulator(θ_test, m);\nassessment = assess(estimator, θ_test, Z_test)\nplot(assessment)\n\n# Inference with observed data \nθ = [0.8f0 0.1f0]'\nZ = simulator(θ, m)\nsampleposterior(estimator, Z) # posterior draws \nposteriormean(estimator, Z)   # point estimate\n\n\n\n\n\n","category":"type"},{"location":"API/core/#NeuralEstimators.RatioEstimator","page":"Core","title":"NeuralEstimators.RatioEstimator","text":"RatioEstimator <: NeuralEstimator\nRatioEstimator(summary_network, num_parameters; num_summaries, kwargs...)\n\nA neural estimator that estimates the likelihood-to-evidence ratio,\n\nr(boldsymbolZ boldsymboltheta) equiv p(boldsymbolZ mid boldsymboltheta)p(boldsymbolZ)\n\nwhere p(boldsymbolZ mid boldsymboltheta) is the likelihood and p(boldsymbolZ) is the marginal likelihood, also known as the model evidence.\n\nThe estimator jointly summarises the data boldsymbolZ and parameters boldsymboltheta  using separate summary networks, whose outputs are concatenated and passed to an MLP inference network.  The parameter summary network maps boldsymboltheta to a vector of 2 * num_parameters summaries by default.\n\nFor numerical stability, training is done on the log-scale using the relation  log r(boldsymbolZ boldsymboltheta) = textlogit(c^*(boldsymbolZ boldsymboltheta)),  where c^*(cdot cdot) denotes the Bayes classifier as described in the methodology section. \n\nGiven data Z and parameters θ, the estimated ratio can be obtained using logratio  and can be used in various Bayesian (e.g., Hermans et al., 2020) or frequentist (e.g., Walchessen et al., 2024) inferential algorithms. For Bayesian inference, posterior samples can be obtained via simple grid-based sampling using sampleposterior.\n\nKeyword arguments\n\nnum_summaries::Integer: the number of summaries output by summary_network. Must match the output dimension of summary_network.\nnum_summaries_θ::Integer = 2 * num_parameters: the number of summaries output by the parameter summary network.\nsummary_network_θ_kwargs::NamedTuple = (;): keyword arguments passed to the MLP constructor for the parameter summary network.\nkwargs...: additional keyword arguments passed to the MLP constructor for the inference network.\n\nExamples\n\nusing NeuralEstimators, Flux, CairoMakie\n\n# Data Z|μ,σ ~ N(μ, σ²) with priors μ ~ U(0, 1) and σ ~ U(0, 1)\nnum_parameters = 2     # dimension of the parameter vector θ\nn = 1                  # dimension of each independent replicate of Z\nm = 30                 # number of independent replicates in each data set\nsampler(K) = rand32(num_parameters, K)\nsimulator(θ, m) = [ϑ[1] .+ ϑ[2] .* randn32(n, m) for ϑ in eachcol(θ)]\n\n# Summary network\nnum_summaries = 4num_parameters\nw = 128   \nψ = Chain(Dense(n, w, relu), Dense(w, w, relu), Dense(w, w, relu))\nϕ = Chain(Dense(w, w, relu), Dense(w, w, relu), Dense(w, num_summaries))\nsummary_network = DeepSet(ψ, ϕ)\n\n# Initialise the estimator\nestimator = RatioEstimator(summary_network, num_parameters; num_summaries = num_summaries)\n\n# Train the estimator\nestimator = train(estimator, sampler, simulator, simulator_args = m, K = 1000)\n\n# Plot the risk history\nplotrisk()\n\n# Assess the estimator\nθ_test = sampler(500)\nZ_test = simulator(θ_test, m);\nθ_grid = f32(expandgrid(0:0.01:1, 0:0.01:1))'  # fine gridding of the parameter space\nassessment = assess(estimator, θ_test, Z_test; θ_grid = θ_grid)\nplot(assessment)\n\n# Generate \"observed\" data \nθ = sampler(1)\nz = simulator(θ, 200)\n\n# Grid-based optimization and sampling\nlogratio(estimator, z, θ_grid = θ_grid)                # log of likelihood-to-evidence ratios\nposteriormode(estimator, z; θ_grid = θ_grid)           # posterior mode \nsampleposterior(estimator, z; θ_grid = θ_grid)         # posterior sample\n\n# Gradient-based optimization\nusing Optim\nθ₀ = [0.5, 0.5]                                        # initial estimate\nposteriormode(estimator, z; θ₀ = θ₀)                   # posterior mode \n\n\n\n\n\n","category":"type"},{"location":"API/core/#NeuralEstimators.IntervalEstimator","page":"Core","title":"NeuralEstimators.IntervalEstimator","text":"IntervalEstimator <: BayesEstimator\nIntervalEstimator(u, v = u, c::Union{Function, Compress} = identity; probs = [0.025, 0.975], g = exp)\nIntervalEstimator(u, c::Union{Function, Compress}; probs = [0.025, 0.975], g = exp)\n\nA neural estimator that jointly estimates marginal posterior credible intervals based on the probability levels probs (by default, 95% central credible intervals).\n\nThe estimator employs a representation that prevents quantile crossing. Specifically, given data boldsymbolZ,  it constructs intervals for each parameter theta_i, i = 1 dots d  of the form,\n\nc_i(u_i(boldsymbolZ))  c_i(u_i(boldsymbolZ)) + g(v_i(boldsymbolZ)))\n\nwhere  boldsymbolu() equiv (u_1(cdot) dots u_d(cdot)) and boldsymbolv() equiv (v_1(cdot) dots v_d(cdot)) are neural networks that map from the sample space to mathbbR^d; g(cdot) is a monotonically increasing function (e.g., exponential or softplus); and each c_i() is a monotonically increasing function that maps its input to the prior support of theta_i.\n\nThe functions c_i() may be collectively defined by a d-dimensional Compress object, which can constrain the interval estimator's output to the prior support. If these functions are unspecified, they will be set to the identity function so that the range of the intervals will be unrestricted. If only a single neural-network architecture is provided, it will be used for both boldsymbolu() and boldsymbolv().\n\nThe return value when applied to data using estimate() is a matrix with 2d rows, where the first and second d rows correspond to the lower and upper bounds, respectively. The function interval() can be used to format this output in a readable d × 2 matrix.  \n\nSee also QuantileEstimator.\n\nExamples\n\nusing NeuralEstimators, Flux, CairoMakie\n\n# Data Z|μ,σ ~ N(μ, σ²) with priors μ ~ U(0, 1) and σ ~ U(0, 1)\nd = 2     # dimension of the parameter vector θ\nn = 1     # dimension of each independent replicate of Z\nm = 100   # number of independent replicates\nsample(K) = rand32(d, K)\nsimulate(θ, m) = [ϑ[1] .+ ϑ[2] .* randn(n, m) for ϑ in eachcol(θ)]\n\n# Neural network\nw = 128   # width of each hidden layer\nψ = Chain(Dense(n, w, relu), Dense(w, w, relu))\nϕ = Chain(Dense(w, w, relu), Dense(w, d))\nu = DeepSet(ψ, ϕ)\n\n# Initialise the estimator\nestimator = IntervalEstimator(u)\n\n# Train the estimator\nestimator = train(estimator, sample, simulate, simulator_args = m, K = 3000)\n\n# Plot the risk history\nplotrisk()\n\n# Assess the estimator\nθ_test = sample(500)\nZ_test = simulate(θ_test, m);\nassessment = assess(estimator, θ_test, Z_test)\nplot(assessment)\n\n# Inference with \"observed\" data \nθ = [0.8f0; 0.1f0]\nZ = simulate(θ, m)\nestimate(estimator, Z) \ninterval(estimator, Z)\n\n\n\n\n\n","category":"type"},{"location":"API/core/#NeuralEstimators.QuantileEstimator","page":"Core","title":"NeuralEstimators.QuantileEstimator","text":"QuantileEstimator <: BayesEstimator\nQuantileEstimator(v; probs = [0.025, 0.5, 0.975], g = Flux.softplus, i = nothing)\n\nA neural estimator that jointly estimates a fixed set of marginal posterior quantiles, with probability levels tau_1 dots tau_T controlled by the keyword argument probs. This generalises IntervalEstimator to support an arbitrary number of probability levels. \n\nGiven data boldsymbolZ, by default the estimator approximates quantiles of the distributions of \n\ntheta_i mid boldsymbolZ quad i = 1 dots d \n\nfor parameters boldsymboltheta equiv (theta_1 dots theta_d). Alternatively, if initialised with i set to a positive integer, the estimator approximates quantiles of the full conditional distribution of  \n\ntheta_i mid boldsymbolZ boldsymboltheta_-i\n\nwhere boldsymboltheta_-i denotes the parameter vector with its ith element removed. \n\nThe estimator employs a representation that prevents quantile crossing, namely,\n\nbeginaligned\nboldsymbolq^(tau_1)(boldsymbolZ) = boldsymbolv^(tau_1)(boldsymbolZ)\nboldsymbolq^(tau_t)(boldsymbolZ) = boldsymbolv^(tau_1)(boldsymbolZ) + sum_j=2^t g(boldsymbolv^(tau_j)(boldsymbolZ)) quad t = 2 dots T\nendaligned\n\nwhere boldsymbolq^(tau)(boldsymbolZ) denotes the vector of tau-quantiles  for parameters boldsymboltheta equiv (theta_1 dots theta_d);  boldsymbolv^(tau_t)(cdot), t = 1 dots T, are neural networks that map from the sample space to mathbbR^d; and g(cdot) is a monotonically increasing function (e.g., exponential or softplus) applied elementwise to its arguments. If g = nothing, the quantiles are estimated independently through the representation\n\nboldsymbolq^(tau_t)(boldsymbolZ) = boldsymbolv^(tau_t)(boldsymbolZ) quad t = 1 dots T\n\nWhen the neural networks are DeepSet objects, two requirements must be met.  First, the number of input neurons in the first layer of the outer network must equal the number of neurons in the final layer of the inner network plus textdim(boldsymboltheta_-i), where we define  textdim(boldsymboltheta_-i) equiv 0 when targetting marginal posteriors of the form theta_i mid boldsymbolZ (the default behaviour).  Second, the number of output neurons in the final layer of the outer network must equal d - textdim(boldsymboltheta_-i). \n\nThe return value is a matrix with d - textdim(boldsymboltheta_-i) times T rows, where the first T rows correspond to the estimated quantiles for the first parameter, the second T rows corresponds to the estimated quantiles for the second parameter, and so on.\n\nExamples\n\nusing NeuralEstimators, Flux\n\n# Data Z|μ,σ ~ N(μ, σ²) with priors μ ~ U(0, 1) and σ ~ U(0, 1)\nd = 2     # dimension of the parameter vector θ\nn = 1     # dimension of each independent replicate of Z\nm = 30    # number of independent replicates in each data set\nsample(K) = rand32(d, K)\nsimulate(θ, m) = [ϑ[1] .+ ϑ[2] .* randn32(n, m) for ϑ in eachcol(θ)]\n\n# ---- Quantiles of θᵢ ∣ 𝐙, i = 1, …, d ----\n\n# Neural network\nw = 64   # width of each hidden layer\nψ = Chain(Dense(n, w, relu), Dense(w, w, relu))\nϕ = Chain(Dense(w, w, relu), Dense(w, d))\nv = DeepSet(ψ, ϕ)\n\n# Initialise the estimator\nestimator = QuantileEstimator(v)\n\n# Train the estimator\nestimator = train(estimator, sample, simulate, simulator_args = m)\n\n# Inference with \"observed\" data \nθ = [0.8f0; 0.1f0]\nZ = simulate(θ, m)\nestimate(estimator, Z) \n\n# ---- Quantiles of θᵢ ∣ 𝐙, θ₋ᵢ ----\n\n# Neural network\nw = 64  # width of each hidden layer\nψ = Chain(Dense(n, w, relu), Dense(w, w, relu))\nϕ = Chain(Dense(w + 1, w, relu), Dense(w, d - 1))\nv = DeepSet(ψ, ϕ)\n\n# Initialise estimators respectively targetting quantiles of μ∣Z,σ and σ∣Z,μ\nq₁ = QuantileEstimator(v; i = 1)\nq₂ = QuantileEstimator(v; i = 2)\n\n# Train the estimators\nq₁ = train(q₁, sample, simulate, simulator_args = m)\nq₂ = train(q₂, sample, simulate, simulator_args = m)\n\n# Estimate quantiles of μ∣Z,σ with σ = 0.5 and for many data sets\nθ₋ᵢ = 0.5f0\nq₁(Z, θ₋ᵢ)\n\n# Estimate quantiles of μ∣Z,σ with σ = 0.5 for a single data set\nq₁(Z[1], θ₋ᵢ)\n\n\n\n\n\n","category":"type"},{"location":"API/core/#NeuralEstimators.Ensemble","page":"Core","title":"NeuralEstimators.Ensemble","text":"Ensemble <: NeuralEstimator\nEnsemble(estimators)\nEnsemble(architecture::Function, J::Integer)\n(ensemble::Ensemble)(Z; aggr = mean)\n\nDefines an ensemble of estimators which, when applied to data Z, returns the mean (or another summary defined by aggr) of the individual estimates (see, e.g., Sainsbury-Dale et al., 2025, Sec. S5).\n\nThe ensemble can be initialised with a collection of trained estimators and then applied immediately to observed data. Alternatively, the ensemble can be initialised with a collection of untrained estimators (or a function defining the architecture of each estimator, and the number of estimators in the ensemble), trained with train(), and then applied to observed data. In the latter case, where the ensemble is trained directly, if savepath is specified both the ensemble and component estimators will be saved.\n\nNote that train() currently acts sequentially on the component estimators, using the Adam optimiser.\n\nThe ensemble components can be accessed by indexing the ensemble; the number of component estimators can be obtained using length().\n\nSee also Parallel, which can be used to mimic ensemble methods with an appropriately chosen connection. \n\nExamples\n\nusing NeuralEstimators, Flux\n\n# Data Z|θ ~ N(θ, 1) with θ ~ N(0, 1)\nd = 1     # dimension of the parameter vector θ\nn = 1     # dimension of each independent replicate of Z\nm = 30    # number of independent replicates in each data set\nsampler(K) = randn32(d, K)\nsimulator(θ, m) = [μ .+ randn32(n, m) for μ ∈ eachcol(θ)]\n\n# Neural-network architecture of each ensemble component\nfunction architecture()\n\tψ = Chain(Dense(n, 64, relu), Dense(64, 64, relu))\n\tϕ = Chain(Dense(64, 64, relu), Dense(64, d))\n\tnetwork = DeepSet(ψ, ϕ)\n\tPointEstimator(network)\nend\n\n# Initialise ensemble with three component estimators \nensemble = Ensemble(architecture, 3)\nensemble[1]      # access component estimators by indexing\nensemble[1:2]    # indexing with an iterable collection returns the corresponding ensemble \nlength(ensemble) # number of component estimators\n\n# Training\nensemble = train(ensemble, sampler, simulator, m = m, epochs = 5)\n\n# Assessment\nθ = sampler(1000)\nZ = simulator(θ, m)\nassessment = assess(ensemble, θ, Z)\nrmse(assessment)\n\n# Apply to data\nensemble(Z)\n\n\n\n\n\n","category":"type"},{"location":"API/core/#NeuralEstimators.summarynetwork","page":"Core","title":"NeuralEstimators.summarynetwork","text":"summarynetwork(estimator::NeuralEstimator)\n\nReturns the summary network of estimator.\n\nSee also summarystatistics.\n\n\n\n\n\n","category":"function"},{"location":"API/core/#NeuralEstimators.setsummarynetwork","page":"Core","title":"NeuralEstimators.setsummarynetwork","text":"setsummarynetwork(estimator::NeuralEstimator, network)\n\nReturns a new estimator identical to estimator but with the summary network replaced by network. Useful for transfer learning.\n\nNote that RatioEstimator has a second summary network for the parameters, accessible via estimator.summary_network_θ, which is not affected by this function.\n\nSee also summarynetwork.\n\n\n\n\n\n","category":"function"},{"location":"API/core/#NeuralEstimators.summarystatistics","page":"Core","title":"NeuralEstimators.summarystatistics","text":"summarystatistics(estimator::NeuralEstimator, Z; batchsize::Integer = 32, use_gpu::Bool = true)\n\nComputes learned summary statistics by applying the summary network of estimator to data Z.\n\nIf Z is a DataSet object, the learned summary statistics are concatenated with the precomputed expert summary statistics stored in Z.S.\n\nSee also summarynetwork.\n\n\n\n\n\n","category":"function"},{"location":"API/core/#NeuralEstimators.logdensity","page":"Core","title":"NeuralEstimators.logdensity","text":"logdensity(estimator::PosteriorEstimator, θ, Z)\n\nEvaluates the log-density of the approximate posterior implied by estimator at parameters θ given data Z,\n\nlog q(boldsymboltheta mid boldsymbolZ)\n\nwhere q denotes the approximate posterior distribution.\n\nθ should be a d times K matrix of parameter vectors and Z a collection of K data sets.\n\nSee also sampleposterior.\n\n\n\n\n\n","category":"function"},{"location":"API/core/#NeuralEstimators.train","page":"Core","title":"NeuralEstimators.train","text":"train(estimator, θ_train::P, θ_val::P, Z_train::T, Z_val::T; ...) where {T, P <: Union{AbstractMatrix, AbstractParameterSet}}\ntrain(estimator, θ_train::P, θ_val::P, simulator::Function; ...) where {P <: Union{AbstractMatrix, AbstractParameterSet}}\ntrain(estimator, sampler::Function, simulator::Function; ...)\n\nTrains a neural estimator.\n\nThe methods cater for different variants of \"on-the-fly\" simulation. Specifically, a sampler can be provided to continuously sample new parameter vectors from the prior, and a simulator can be provided to continuously simulate new data conditional on the parameters. If provided with specific sets of parameters (θ_train and θ_val) and/or data (Z_train and Z_val), they will be held fixed during training.\n\nThe validation parameters and data are always held fixed, \n\nThe trained estimator is always returned on the CPU. \n\nKeyword arguments common to all methods:\n\nloss = Flux.mae (applicable only to PointEstimator): loss function used to train the neural network. \nepochs = 100: number of epochs to train the neural network. An epoch is one complete pass through the entire training data set when doing stochastic gradient descent.\nstopping_epochs = 5: cease training if the risk does not improve in this number of epochs.\nbatchsize = 32: the batchsize to use when performing stochastic gradient descent, that is, the number of training samples processed between each update of the neural-network parameters.\noptimiser = Flux.setup(Adam(5e-4), estimator): any Optimisers.jl optimisation rule for updating the neural-network parameters. When the training data and/or parameters are held fixed, one may wish to employ regularisation to prevent overfitting; for example, optimiser = Flux.setup(OptimiserChain(WeightDecay(1e-4), Adam()), estimator), which corresponds to L₂ regularisation with penalty coefficient λ=10⁻⁴. \nlr_schedule::Union{Nothing, ParameterSchedulers.AbstractSchedule}: defines the learning-rate schedule for adaptively changing the learning rate during training. Accepts either a ParameterSchedulers.jl object or nothing for a fixed learning rate. By default, it uses CosAnneal with a maximum set to the initial learning rate from optimiser, a minimum of zero, and a period equal to the number of epochs. The learning rate is updated at the end of each epoch. \nfreeze_summary_network = false: if true and the estimator has a summary_network field, freezes the summary network parameters during training (i.e., only the inference network is updated). This is useful for transfer learning, where a pretrained summary network is held fixed while a new inference network is trained for a different model or estimator type.\nuse_gpu = true: flag indicating whether to use a GPU if one is available.\nsavepath::Union{Nothing, String} = tempdir(): path to save information generated during training. If nothing, nothing is saved. Otherwise, the following files are always saved to both savepath and tempdir() (the latter for convenient within-session access via loadrisk, plotrisk, and loadoptimiser):\nloss_per_epoch.csv: training and validation risk at each epoch, in the first and second columns respectively.\nbest_optimiser.bson: optimiser state corresponding to the best validation risk.\nfinal_optimiser.bson: optimiser state at the final epoch.\nIf additionally savepath != tempdir(), the following files are also saved to savepath:\nbest_network.bson: neural-network parameters corresponding to the best validation risk.\nfinal_network.bson: neural-network parameters at the final epoch.\ntrain_time.csv: total training time in seconds.\nrisk_history::Union{Nothing, Matrix} = nothing: a matrix with two columns containing the training and validation risk from a previous call to train(), used to initialise the risk history when continuing training. Can be loaded from a previous call to train using loadrisk.\nverbose = true: flag indicating whether information, including empirical risk values and timings, should be printed to the console during training.\n\nKeyword arguments common to train(estimator, sampler, simulator) and train(estimator, θ_train, θ_val, simulator):\n\nsimulator_args = (): positional arguments passed to the simulator as simulator(θ, simulator_args...).\nsimulator_kwargs::NamedTuple = (;): keyword arguments passed to the simulator as simulator(...; simulator_kwargs...).\nepochs_per_Z_refresh = 1: the number of passes to make through the training set before the training data are refreshed.\nsimulate_just_in_time = false: flag indicating whether we should simulate just-in-time, in the sense that only a batchsize number of parameter vectors and corresponding data are in memory at a given time.\n\nKeyword arguments unique to train(estimator, sampler, simulator):\n\nsampler_args = (): positional arguments passed to the parameter sampler as sampler(K, sampler_args...).\nsampler_kwargs::NamedTuple = (;): keyword arguments passed to the parameter sampler as sampler(...; sampler_kwargs...).\nK = 10000: number of parameter vectors in the training set.\nK_val = K ÷ 2 number of parameter vectors in the validation set.\nepochs_per_θ_refresh = 1: the number of passes to make through the training set before the training parameters are refreshed. Must be a multiple of epochs_per_Z_refresh. Can also be provided as epochs_per_theta_refresh.\n\nExamples\n\nusing NeuralEstimators, Flux\n\n# Data Z|μ,σ ~ N(μ, σ²) with priors μ ~ N(0, 1) and σ ~ U(0, 1)\nfunction sampler(K)\n\tμ = randn(K) # Gaussian prior\n\tσ = rand(K)  # Uniform prior\n\tθ = vcat(μ', σ')\n\treturn θ\nend\nfunction simulator(θ, m)\n\t[ϑ[1] .+ ϑ[2] * randn(1, m) for ϑ ∈ eachcol(θ)]\nend\nd = 2     # dimension of the parameter vector θ\nn = 1     # dimension of each independent replicate of Z\n\n# Summary network\nnum_summaries = 4d\nw = 64 # width of each hidden layer  \nψ = Chain(Dense(n, w, relu), Dense(w, w, relu), Dense(w, w, relu))\nϕ = Chain(Dense(w, w, relu), Dense(w, w, relu), Dense(w, num_summaries))\nsummary_network = DeepSet(ψ, ϕ)\n\n# Initialise the estimator\nestimator = PointEstimator(summary_network, num_summaries, d)\n\n# Number of data sets in each epoch and number of independent replicates in each data set\nK = 1000\nm = 30\n\n# Training: simulation on-the-fly\nestimator  = train(estimator, sampler, simulator, simulator_args = m, K = K)\n\n# Plot the risk history (using any plotting backend)\nusing Plots\nunicodeplots()\nplotrisk()\n\n# Training: simulation on-the-fly with fixed parameters \nθ_train = sampler(K)\nθ_val   = sampler(K)\nestimator = train(estimator, θ_train, θ_val, simulator, simulator_args = m, optimiser = loadoptimiser(), risk_history = loadrisk())\n\n# Training: fixed parameters and fixed data\nZ_train   = simulator(θ_train, m)\nZ_val     = simulator(θ_val, m)\nestimator = train(estimator, θ_train, θ_val, Z_train, Z_val, optimiser = loadoptimiser(), risk_history = loadrisk())\n\n\n\n\n\n","category":"function"},{"location":"API/core/#NeuralEstimators.loadrisk","page":"Core","title":"NeuralEstimators.loadrisk","text":"loadrisk(savepath::String = tempdir())\n\nLoads the training and validation risk history saved during the most recent call to train(). By default, loads from the temporary directory used during the current session. If a savepath was provided to train(), that path can be passed here to reload risk history from a previous session.\n\nReturns a matrix with two columns: training risk (column 1) and validation risk (column 2).\n\nSee also plotrisk.\n\n\n\n\n\n","category":"function"},{"location":"API/core/#NeuralEstimators.plotrisk","page":"Core","title":"NeuralEstimators.plotrisk","text":"plotrisk(savepath::String = tempdir())\n\nPlots the training and validation risk as a function of epoch, loaded from savepath. By default, loads from the temporary directory used during the current session. If a savepath was provided to train(), that path can be passed here to plot risk history from a previous session.\n\nRequires a plotting package (e.g., using Plots) to be loaded.\n\nSee also loadrisk.\n\n\n\n\n\n","category":"function"},{"location":"API/core/#NeuralEstimators.loadoptimiser","page":"Core","title":"NeuralEstimators.loadoptimiser","text":"loadoptimiser(savepath::String = tempdir(); best::Bool = true)\n\nLoads the optimiser saved during the most recent call to train().\n\nBy default, loads from the temporary directory used during the current session. If a savepath was provided to train(), that path can be passed here to reload the optimiser from a previous session. \n\nBy default, loads the optimiser corresponding to the best network (as measured by validation loss). Set best = false to load the optimiser from the final epoch instead, which can be useful for resuming training from exactly where it left off.\n\nThe returned optimizer can be passed directly to train() via the optimiser  keyword argument to resume training with the correct optimiser state.\n\n\n\n\n\n","category":"function"},{"location":"API/core/#NeuralEstimators.assess","page":"Core","title":"NeuralEstimators.assess","text":"assess(estimator, θ, Z; ...)\nassess(estimators::Vector, θ, Z; ...)\n\nAssesses an estimator (or a collection of estimators) based on true parameters θ and corresponding simulated data Z.\n\nThe parameters θ should be given as a d × K matrix, where d is the parameter dimension and K is the number of sampled parameter vectors.\n\nWhen Z contains more simulated data sets than the number K of sampled parameter vectors, θ will be recycled via horizontal concatenation: θ = repeat(θ, outer = (1, J)), where J = numobs(Z) ÷ K is the number of simulated data sets for each parameter vector. This allows assessment of the estimator's sampling distribution under fixed parameters.\n\nThe return value is of type Assessment.\n\nKeyword arguments\n\nestimator_name::String (or estimator_names::Vector{String} for multiple estimators): name(s) of the estimator(s) (sensible defaults provided).\nparameter_names::Vector{String}: names of the parameters (sensible default provided).\nuse_gpu = true: Bool or Vector{Bool} with length equal to the number of estimators.\nprobs = nothing (applicable only to PointEstimator): probability levels taking values between 0 and 1. By default, no bootstrap uncertainty quantification is done; if probs is provided, it must be a two-element vector specifying the lower and upper probability levels for non-parametric bootstrap intervals (note that parametric bootstrap is not currently supported with assess()).\nB::Integer = 400 (applicable only to PointEstimator): number of bootstrap samples.\npointsummary::Function = mean (applicable only to estimators that yield posterior samples): a function that summarises a vector of posterior samples into a single point estimate for each marginal; any function mapping a vector to a scalar is valid (e.g., median for the posterior median).\nN::Integer = 1000 (applicable only to estimators that yield posterior samples): number of posterior samples drawn for each data set.\nkwargs... (applicable only to estimators that yield posterior samples): additional keyword arguments passed to sampleposterior.\n\n\n\n\n\n","category":"function"},{"location":"API/core/#NeuralEstimators.Assessment","page":"Core","title":"NeuralEstimators.Assessment","text":"Assessment\n\nA type for storing the output of assess(). The field runtime contains the total time taken for each estimator. The field estimates is a long-form DataFrame with columns:\n\nparameter: the name of the parameter\ntruth:     the true value of the parameter\nestimate:  the estimated value of the parameter\nk:         the index of the parameter vector\nj:         the index of the data set (only relevant in the case that multiple data sets are associated with each parameter vector)\n\nIf the estimator is an IntervalEstimator, the column estimate will be replaced by the columns lower and upper, containing the lower and upper bounds of the interval, respectively.\n\nIf the estimator is a QuantileEstimator, there will also be a column prob indicating the probability level of the corresponding quantile estimate.\n\nIf the estimator is a PosteriorEstimator, in addition to the fields listed above, the field samples stores the posterior samples as a long-form DataFrame with the columns parameter, truth, k, j (as given above), as well as:\n\ndraw: the index of the draw within the posterior samples\nvalue: the value of the posterior sample for a given parameter and draw.\n\nUse merge() to combine assessments from multiple estimators of the same type or join() to combine assessments from a PointEstimator and an IntervalEstimator.\n\n\n\n\n\n","category":"type"},{"location":"API/core/#Makie.plot-Tuple{Assessment}","page":"Core","title":"Makie.plot","text":"plot(assessment::Assessment; prob = 0.99)\n\nVisualise the performance of a neural estimator. Accepts the Assessment object returned by assess.\n\nnote: Extension\nThis function is defined in the NeuralEstimatorsPlottingMakieExt extension and requires CairoMakie (or another Makie backend) to be loaded.\n\nThe plot produced depends on the type of estimator being assessed:\n\nPointEstimator: produces a scatter plot of estimates vs. true values, faceted by parameter.\n\nIntervalEstimator: produces a plot of estimated credible intervals vs. true values, faceted by parameter. Each interval is drawn as a vertical line segment from lower to upper bound, with tick marks at the endpoints.\n\nQuantileEstimator: produces a calibration plot of the empirical coverage probability vs. the nominal probability level τ, faceted by parameter. A well-calibrated estimator will follow the red diagonal line. Specifically, the diagnostic is constructed as follows:\n\nFor k = 1,…,K, sample pairs (θᵏ, Zᵏ) with θᵏ ∼ p(θ), Zᵏ ∼ p(Z ∣ θᵏ). This gives K \"posterior draws\", θᵏ ∼ p(θ ∣ Zᵏ).\nFor each k and each τ ∈ {τⱼ : j = 1,…,J}, estimate the posterior quantile Q(Zᵏ, τ).\nFor each τ, compute the proportion of quantiles Q(Zᵏ, τ) exceeding the corresponding θᵏ, and plot this proportion against τ.\n\nPosteriorEstimator: produces a three-row figure:\n\nRecovery plot: posterior mean vs. true value (scatter), with vertical line segments showing the 95% posterior credible interval, faceted by parameter.\nECDF plot: for each parameter, the empirical CDF of the fractional rank of the true value within the posterior samples, together with a simultaneous prob-level confidence band. A well-calibrated posterior yields an ECDF that stays within the band.\nZ-score / contraction plot: posterior z-score (posterior mean − truth) / posterior SD vs. posterior contraction 1 − Var(posterior) / Var(prior), faceted by parameter. Ideally z-scores are centred near zero and contractions are near one.\n\nKeyword arguments\n\nprob = 0.99: nominal simultaneous coverage level for the SBC confidence band. Only used when assessment contains posterior samples.\n\n\n\n\n\n","category":"method"},{"location":"API/core/#NeuralEstimators.risk","page":"Core","title":"NeuralEstimators.risk","text":"risk(assessment::Assessment; ...)\n\nComputes a Monte Carlo approximation of an estimator's Bayes risk,\n\nr(hatboldsymboltheta(cdot))\napprox\nfrac1K sum_k=1^K L(boldsymboltheta^(k) hatboldsymboltheta(boldsymbolZ^(k)))\n\nwhere boldsymboltheta^(k)  k = 1 dots K denotes a set of K parameter vectors sampled from the prior and, for each k, data boldsymbolZ^(k) are simulated from the statistical model conditional on boldsymboltheta^(k).\n\nIf the Assessment object corresponds to an estimator with a self-defined loss (e.g., PosteriorEstimator), the precomputed risk is returned directly. Otherwise, the risk is computed from the estimates and true parameters using the provided loss function.\n\nKeyword arguments\n\nloss = (x, y) -> abs(x - y): a binary operator defining the loss function (default: absolute-error loss)\naverage_over_parameters::Bool = false: if true, the loss is averaged over all parameters; otherwise (default), it is computed separately for each parameter.\n\n\n\n\n\n","category":"function"},{"location":"API/core/#NeuralEstimators.bias","page":"Core","title":"NeuralEstimators.bias","text":"bias(assessment::Assessment; average_over_parameters = false)\n\nComputes a Monte Carlo approximation of an estimator's bias,\n\ntextrmbias(hatboldsymboltheta(cdot))\napprox\nfrac1K sum_k=1^K hatboldsymboltheta(boldsymbolZ^(k)) - boldsymboltheta^(k)\n\nwhere boldsymboltheta^(k)  k = 1 dots K denotes a set of K parameter vectors sampled from the prior and, for each k, data boldsymbolZ^(k) are simulated from the statistical model conditional on boldsymboltheta^(k).\n\n\n\n\n\n","category":"function"},{"location":"API/core/#NeuralEstimators.rmse","page":"Core","title":"NeuralEstimators.rmse","text":"rmse(assessment::Assessment; average_over_parameters = false)\n\nComputes a Monte Carlo approximation of an estimator's root-mean-squared error,\n\ntextrmrmse(hatboldsymboltheta(cdot))\napprox\nsqrtfrac1K sum_k=1^K hatboldsymboltheta(boldsymbolZ^(k)) - boldsymboltheta^(k)^2\n\nwhere boldsymboltheta^(k)  k = 1 dots K denotes a set of K parameter vectors sampled from the prior and, for each k, data boldsymbolZ^(k) are simulated from the statistical model conditional on boldsymboltheta^(k).\n\n\n\n\n\n","category":"function"},{"location":"API/core/#NeuralEstimators.coverage","page":"Core","title":"NeuralEstimators.coverage","text":"coverage(assessment::Assessment; ...)\n\nComputes a Monte Carlo approximation of an interval estimator's expected coverage, as defined in Hermans et al. (2022, Definition 2.1), and the proportion of parameters below and above the lower and upper bounds, respectively.\n\nKeyword arguments\n\naverage_over_parameters::Bool = false: if true, the coverage is averaged over all parameters; otherwise (default), it is computed over each parameter separately.\n\n\n\n\n\n","category":"function"},{"location":"API/core/#NeuralEstimators.estimate","page":"Core","title":"NeuralEstimators.estimate","text":"estimate(estimator::BayesEstimator, Z; batchsize::Integer = 32, use_gpu::Bool = true, kwargs...)\n\nApplies estimator to data Z and returns the resulting estimates.\n\n\n\n\n\n","category":"function"},{"location":"API/core/#NeuralEstimators.bootstrap","page":"Core","title":"NeuralEstimators.bootstrap","text":"bootstrap(estimator::PointEstimator, parameters::P, Z; use_gpu = true) where P <: Union{AbstractMatrix, AbstractParameterSet}\nbootstrap(estimator::PointEstimator, parameters::P, simulator, m::Integer; B = 400, use_gpu = true) where P <: Union{AbstractMatrix, AbstractParameterSet}\nbootstrap(estimator::PointEstimator, Z; B = 400, blocks = nothing, trim = true, use_gpu = true)\n\nGenerates B bootstrap estimates using estimator.\n\nParametric bootstrapping is facilitated by passing a single parameter configuration, parameters, and corresponding simulated data, Z, whose length implicitly defines B. Alternatively, one may provide a simulator and the desired sample size, in which case the data will be simulated using simulator(parameters, m).\n\nNon-parametric bootstrapping is facilitated by passing a single data set, Z. The argument blocks caters for block bootstrapping, and it should be a vector of integers specifying the block for each replicate. For example, with 5 replicates, the first two corresponding to block 1 and the remaining three corresponding to block 2, blocks should be [1, 1, 2, 2, 2]. The resampling algorithm generates resampled data sets by sampling blocks with replacement. If trim = true, the final block is trimmed as needed to ensure that the resampled data set matches the original size of Z. \n\nThe return type is a d × B matrix, where d is the dimension of the parameter vector. \n\n\n\n\n\n","category":"function"},{"location":"API/core/#NeuralEstimators.interval","page":"Core","title":"NeuralEstimators.interval","text":"interval(θ::Matrix; probs = [0.05, 0.95], parameter_names = nothing)\ninterval(estimator::IntervalEstimator, Z; parameter_names = nothing, use_gpu = true)\n\nComputes a confidence/credible interval based either on a d × B matrix θ of parameters (typically containing bootstrap estimates or posterior draws), where d denotes the number of parameters to make inference on, or from an IntervalEstimator and data Z.\n\nWhen given θ, the intervals are constructed by computing quantiles with probability levels controlled by the keyword argument probs.\n\nThe return type is a d × 2 matrix, whose first and second columns respectively contain the lower and upper bounds of the interval. The rows of this matrix can be named by passing a vector of strings to the keyword argument parameter_names. \n\n\n\n\n\n","category":"function"},{"location":"API/core/#NeuralEstimators.sampleposterior","page":"Core","title":"NeuralEstimators.sampleposterior","text":"sampleposterior(estimator::PosteriorEstimator, Z, N::Integer = 1000; use_gpu::Bool = true)\nsampleposterior(estimator::RatioEstimator, Z, N::Integer = 1000; θ_grid, logprior::Function = θ -> 0f0, use_gpu::Bool = true)\n\nSamples from the approximate posterior distribution implied by estimator.\n\nThe positional argument N controls the size of the posterior sample.\n\nIf Z represents a single data set as determined by Flux.numobs(), returns a d × N matrix of posterior samples, where d is the dimension of the parameter vector. Otherwise, if Z contains multiple data sets, a vector of matrices will be returned. \n\nWhen using a RatioEstimator, the prior distribution p(boldsymboltheta) is controlled through the keyword argument logprior (by default, a uniform prior is used). The sampling algorithm is based on a fine-gridding of the parameter space, specified through the keyword argument θ_grid. The approximate posterior density is  evaluated over this grid, which is then used to draw samples. This is effective when making inference with a small number of parameters. For models with a large number of parameters, other sampling algorithms (e.g., MCMC) may be needed (please contact the package maintainer).\n\n\n\n\n\n","category":"function"},{"location":"API/core/#NeuralEstimators.posteriormean","page":"Core","title":"NeuralEstimators.posteriormean","text":"posteriormean(θ::AbstractMatrix)\t\nposteriormean(estimator, Z, N::Integer = 1000; kwargs...)\n\nComputes the posterior mean based either on a d × N matrix θ of posterior draws, where d denotes the number of parameters to make inference on, or directly from an estimator that allows for posterior sampling via sampleposterior().\n\nSee also posteriormedian(), posteriormode().\n\n\n\n\n\n","category":"function"},{"location":"API/core/#NeuralEstimators.posteriormedian","page":"Core","title":"NeuralEstimators.posteriormedian","text":"posteriormedian(θ::AbstractMatrix)\t\nposteriormedian(estimator, Z, N::Integer = 1000; kwargs...)\n\nComputes the vector of marginal posterior medians based either on a d × N matrix θ of posterior draws, where d denotes the number of parameters to make inference on, or directly from an estimator that allows for posterior sampling via sampleposterior().\n\nSee also posteriormean(), posteriorquantile().\n\n\n\n\n\n","category":"function"},{"location":"API/core/#NeuralEstimators.posteriorquantile","page":"Core","title":"NeuralEstimators.posteriorquantile","text":"posteriorquantile(θ::AbstractMatrix, probs)\t\nposteriorquantile(estimator, Z, probs, N::Integer = 1000; kwargs...)\n\nComputes the vector of marginal posterior quantiles with (a collection of) probability levels probs, based either on a d × N matrix θ of posterior draws, where d denotes the number of parameters to make inference on, or directly from an estimator that allows for posterior sampling via sampleposterior().\n\nThe return value is a d × length(probs) matrix. \n\nSee also posteriormedian().\n\n\n\n\n\n","category":"function"},{"location":"API/core/#NeuralEstimators.posteriormode","page":"Core","title":"NeuralEstimators.posteriormode","text":"posteriormode(estimator::RatioEstimator, Z; θ₀ = nothing, θ_grid = nothing, logprior::Function = θ -> 0f0, use_gpu = true)\n\nComputes the (approximate) posterior mode (maximum a posteriori estimate) given data boldsymbolZ,\n\nundersetboldsymbolthetamathrmargmax ell(boldsymboltheta  boldsymbolZ) + log p(boldsymboltheta)\n\nwhere ell(cdot  cdot) denotes the approximate log-likelihood function implied by estimator, and p(boldsymboltheta) denotes the prior density function controlled through the keyword argument prior. Note that this estimate can be viewed as an approximate maximum penalised likelihood estimate, with penalty term p(boldsymboltheta). \n\nIf a vector θ₀ of initial parameter estimates is given, the approximate posterior density is maximised by gradient descent (requires Optim.jl to be loaded). Otherwise, if a matrix of parameters θ_grid is given, the approximate posterior density is maximised by grid search.\n\nSee also posteriormedian(), posteriormean().\n\n\n\n\n\n","category":"function"},{"location":"API/core/#NeuralEstimators.logratio","page":"Core","title":"NeuralEstimators.logratio","text":"logratio(estimator::RatioEstimator, Z, θ_grid)\nlogratio(estimator::RatioEstimator, Z; θ_grid)\n\nCompute the log likelihood-to-evidence ratios over a grid of parameter values θ_grid  for the data Z.\n\nArguments\n\nestimator: a RatioEstimator\nZ: observed data\nθ_grid: matrix of parameter values, where each column is a parameter configuration\n\nReturns\n\nA vector of log ratios, one for each column of θ_grid.\n\n\n\n\n\n","category":"function"},{"location":"API/utility/#Miscellaneous","page":"Miscellaneous","title":"Miscellaneous","text":"Order = [:type, :function]\nPages   = [\"utility.md\"]","category":"section"},{"location":"API/utility/#Core","page":"Miscellaneous","title":"Core","text":"These functions can appear during the core workflow, and may need to be overloaded in some applications.","category":"section"},{"location":"API/utility/#Downstream-inference-algorithms","page":"Miscellaneous","title":"Downstream-inference algorithms","text":"","category":"section"},{"location":"API/utility/#Utility-functions","page":"Miscellaneous","title":"Utility functions","text":"","category":"section"},{"location":"API/utility/#NeuralEstimators.numberreplicates","page":"Miscellaneous","title":"NeuralEstimators.numberreplicates","text":"numberreplicates(Z)\n\nGeneric function that returns the number of replicates in a given object. Default implementations are provided for commonly used data formats, namely, data stored as an Array or as a GNNGraph.\n\n\n\n\n\n","category":"function"},{"location":"API/utility/#NeuralEstimators.subsetdata","page":"Miscellaneous","title":"NeuralEstimators.subsetdata","text":"subsetdata(Z::V, i) where {V <: AbstractArray{A}} where {A <: Any}\nsubsetdata(Z::A, i) where {A <: AbstractArray{T, N}} where {T, N}\nsubsetdata(Z::G, i) where {G <: GNNGraph}\n\nReturn replicate(s) i from each data set in Z.\n\nIf working with data that are not covered by the default methods, overload the function with the appropriate type for Z.\n\nFor graphical data, calls getgraph(), where the replicates are assumed be to stored as batched graphs. Since this can be slow, one should consider using a method of train() that does not require the data to be subsetted when working with graphical data (use numberreplicates() to check that the training and validation data sets are equally replicated, which prevents subsetting).\n\nExamples\n\nusing NeuralEstimators\nusing GraphNeuralNetworks\nusing Flux: batch\n\nd = 1  # dimension of the response variable\nn = 4  # number of observations in each realisation\nm = 6  # number of replicates in each data set\nK = 2  # number of data sets\n\n# Array data\nZ = [rand(n, d, m) for k ∈ 1:K]\nsubsetdata(Z, 2)   # extract second replicate from each data set\nsubsetdata(Z, 1:3) # extract first 3 replicates from each data set\n\n# Graphical data\ne = 8 # number of edges\nZ = [batch([rand_graph(n, e, ndata = rand(d, n)) for _ ∈ 1:m]) for k ∈ 1:K]\nsubsetdata(Z, 2)   # extract second replicate from each data set\nsubsetdata(Z, 1:3) # extract first 3 replicates from each data set\n\n\n\n\n\n","category":"function"},{"location":"API/utility/#NeuralEstimators.subsetparameters","page":"Miscellaneous","title":"NeuralEstimators.subsetparameters","text":"subsetparameters(parameters::M, indices) where {M <: AbstractMatrix}\nsubsetparameters(parameters::P, indices) where {P <: AbstractParameterSet}\n\nSubset parameters using a collection of indices.\n\nArrays in parameters::P with last dimension equal in size to the number of parameter configurations, K, are also subsetted (over their last dimension) using indices. All other fields are left unchanged. To modify this default behaviour, overload subsetparameters.\n\n\n\n\n\n","category":"function"},{"location":"API/utility/#NeuralEstimators.EM","page":"Miscellaneous","title":"NeuralEstimators.EM","text":"EM(simulateconditional::Function, MAP::Union{Function, NeuralEstimator}, θ₀ = nothing)\n\nImplements a (Bayesian) Monte Carlo expectation-maximization (EM) algorithm for  parameter estimation with missing data. The algorithm iteratively simulates missing  data conditional on current parameter estimates, then updates parameters using a  (neural) maximum a posteriori (MAP) estimator.\n\nThe lth iteration is given by:\n\nboldsymboltheta^(l) =\nundersetboldsymbolthetamathrmargmax\nsum_h = 1^H ell(boldsymboltheta  boldsymbolZ_1  boldsymbolZ_2^(l h)) + Hlog pi(boldsymboltheta)\n\nwhere ell((boldsymboltheta  boldsymbolZ_1  boldsymbolZ_2) denotes the complete-data log-likelihood function,  boldsymbolZ equiv (boldsymbolZ_1 boldsymbolZ_2) denotes the complete  data with boldsymbolZ_1 and boldsymbolZ_2 the observed and missing  components respectively, boldsymbolZ_2^(l h), h = 1 dots H, are simulated  from the distribution of boldsymbolZ_2 mid boldsymbolZ_1 boldsymboltheta^(l-1), and  pi(boldsymboltheta) is the prior density (which can be viewed as a penalty function).\n\nThe algorithm monitors convergence by computing:\n\nmax_i left fracbartheta_i^(l+1) - bartheta_i^(l)bartheta_i^(l) + epsilon right\n\nwhere bartheta^(l) is the average of parameter estimates from iteration  burnin+1 to iteration l, and epsilon is machine precision. Convergence is  declared when this quantity is less than tol for nconsecutive consecutive iterations (see keyword arguments below).\n\nFields\n\nsimulateconditional::Function: Function for simulating missing data conditional on  observed data and current parameter estimates. Must have signature:\nsimulateconditional(Z::AbstractArray{Union{Missing, T}}, θ; nsims = 1, kwargs...)\nReturns completed data in the format expected by MAP (e.g., 4D array for CNNs).\nMAP::NeuralEstimator: MAP estimator applied to completed data. \nθ₀: Optional initial parameter values (vector). Can also be provided when calling  the EM object.\n\nMethods\n\nOnce constructed, objects of type EM can be applied to data via the following methods (corresponding to single or multiple data sets, respectively):\n\n(em::EM)(Z::A, θ₀::Union{Nothing, Vector} = nothing; ...) where {A <: AbstractArray{Union{Missing, T}, N}} where {T, N}\n(em::EM)(Z::V, θ₀::Union{Nothing, Vector, Matrix} = nothing; ...) where {V <: AbstractVector{A}} where {A <: AbstractArray{Union{Missing, T}, N}} where {T, N}\n\nwhere Z is the complete data containing the observed data and Missing values.\n\nFor multiple datasets, θ₀ can be a vector (same initial values for all) or a matrix  with one column per dataset.\n\nKeyword Arguments\n\nniterations::Integer = 50: Maximum number of EM iterations.\nnsims::Union{Integer, Vector{<:Integer}} = 1: Number of conditional simulations per  iteration. Can be fixed (scalar) or varying (vector of length niterations).\nburnin::Integer = 1: Number of initial iterations to discard before averaging  parameter estimates for convergence assessment.\nnconsecutive::Integer = 3: Number of consecutive iterations meeting the convergence  criterion required to halt.\ntol = 0.01: Convergence tolerance. Algorithm stops if the relative change in  post-burnin averaged parameters is less than tol for nconsecutive iterations.\nuse_gpu::Bool = true: Whether to use a GPU (if available) for MAP estimation.\nverbose::Bool = false: Whether to print iteration details.\nkwargs...: Additional arguments passed to simulateconditional.\n\nReturns\n\nFor a single data set, returns a named tuple containing:\n\nestimate: Final parameter estimate (post-burnin average).\niterates: Matrix of all parameter estimates across iterations (each column is one iteration).\nburnin: The burnin value used.\n\nFor multiple data set, returns a matrix with one column per dataset.\n\nNotes\n\nIf Z contains no missing values, the MAP estimator is applied directly (after  passing through simulateconditional to ensure correct format).\nWhen using a GPU, data are moved to the GPU for MAP estimation and back to the CPU  for conditional simulation.\n\nExamples\n\n# Below we give a pseudocode example; see the \"Missing data\" section in \"Advanced usage\" for a concrete example.\n\n# Define conditional simulation function\nfunction sim_conditional(Z, θ; nsims = 1)\n    # Your implementation here\n    # Returns completed data in format suitable for MAP estimator\nend\n\n# Define or load MAP estimator\nMAP_estimator = ... # Neural MAP estimator\n\n# Create EM object\nem = EM(sim_conditional, MAP_estimator, θ₀ = [1.0, 2.0])\n\n# Apply to data with missing values\nZ = ... # Array with Missing entries\nresult = em(Z, niterations = 100, nsims = 5, tol = 0.001, verbose = true)\n\n# Access results\nθ_final = result.estimate\nθ_sequence = result.iterates\n\n# Multiple datasets\nZ_list = [Z1, Z2, Z3]\nestimates = em(Z_list, θ₀ = [1.0, 2.0])  # Matrix with 3 columns\n\n\n\n\n\n","category":"type"},{"location":"API/utility/#NeuralEstimators.adjacencymatrix","page":"Miscellaneous","title":"NeuralEstimators.adjacencymatrix","text":"adjacencymatrix(S::Matrix, k::Integer)\nadjacencymatrix(S::Matrix, r::AbstractFloat)\nadjacencymatrix(S::Matrix, r::AbstractFloat, k::Integer; random = true)\nadjacencymatrix(M::Matrix; k, r, kwargs...)\n\nComputes a spatially weighted adjacency matrix from spatial locations S based  on either the k-nearest neighbours of each location; all nodes within a disc of fixed radius r; or, if both r and k are provided, a subset of k neighbours within a disc of fixed radius r.\n\nIf S is a square matrix, it is treated as a distance matrix; otherwise, it should be an n x d matrix, where n is the number of spatial locations and d is the spatial dimension (typically d = 2). In the latter case, the distance metric is taken to be the Euclidean distance.\n\nTwo subsampling strategies are implemented when choosing a subset of k neighbours within  a disc of fixed radius r. If random=true (default), the neighbours are randomly selected from  within the disc. If random=false, a deterministic algorithm is used  that aims to preserve the distribution of distances within the neighbourhood set, by choosing  those nodes with distances to the central node corresponding to the  0 frac1k frac2k dots frack-1k 1 quantiles of the empirical  distribution function of distances within the disc (this in fact yields up to k+1 neighbours,  since both the closest and furthest nodes are always included). \n\nBy convention with the functionality in GraphNeuralNetworks.jl which is based on directed graphs,  the neighbours of location i are stored in the column A[:, i] where A is the  returned adjacency matrix. Therefore, the number of neighbours for each location is given by collect(mapslices(nnz, A; dims = 1)), and the number of times each node is  a neighbour of another node is given by collect(mapslices(nnz, A; dims = 2)).\n\nBy convention, we do not consider a location to neighbour itself (i.e., the diagonal elements of the adjacency matrix are zero). \n\nExamples\n\nusing NeuralEstimators, Distances, SparseArrays\n\nn = 250\nd = 2\nS = rand(Float32, n, d)\nk = 10\nr = 0.10\n\n# Memory efficient constructors\nadjacencymatrix(S, k)\nadjacencymatrix(S, r)\nadjacencymatrix(S, r, k)\nadjacencymatrix(S, r, k; random = false)\n\n# Construct from full distance matrix D\nD = pairwise(Euclidean(), S, dims = 1)\nadjacencymatrix(D, k)\nadjacencymatrix(D, r)\nadjacencymatrix(D, r, k)\nadjacencymatrix(D, r, k; random = false)\n\n\n\n\n\n","category":"function"},{"location":"API/utility/#NeuralEstimators.containertype","page":"Miscellaneous","title":"NeuralEstimators.containertype","text":"containertype(A::Type)\ncontainertype(::Type{A}) where A <: SubArray\ncontainertype(a::A) where A\n\nReturns the container type of its argument.\n\nIf given a SubArray, returns the container type of the parent array.\n\nExamples\n\na = rand(3, 4)\ncontainertype(a)\ncontainertype(typeof(a))\n[containertype(x) for x ∈ eachcol(a)]\n\n\n\n\n\n","category":"function"},{"location":"API/utility/#NeuralEstimators.encodedata","page":"Miscellaneous","title":"NeuralEstimators.encodedata","text":"encodedata(Z::A; c::T = zero(T)) where {A <: AbstractArray{Union{Missing, T}, N}} where T, N\n\nFor data Z with missing entries, returns an encoded data set (U, W) where  U is the original data Z with missing entries replaced by a fixed constant c,  and W encodes the missingness pattern as an indicator array  equal to one if the corresponding element of Z is observed and zero otherwise.\n\nThe behavior depends on the dimensionality of Z. If Z has 1 or 2 dimensions,  the indicator array W is concatenated along the first dimension of Z. If Z has more than 2  dimensions, W is concatenated along the second-to-last dimension of Z. \n\nExamples\n\nusing NeuralEstimators\n\nZ = rand(16, 16, 1, 1)\nZ = removedata(Z, 0.25)\t# remove 25% of the data at random\nUW = encodedata(Z)\n\n\n\n\n\n","category":"function"},{"location":"API/utility/#NeuralEstimators.expandgrid","page":"Miscellaneous","title":"NeuralEstimators.expandgrid","text":"expandgrid(xs, ys)\n\nGenerates a grid of all possible combinations of the elements from two input vectors, xs and ys. \n\nSame as expand.grid() in R, but currently caters for two dimensions only.\n\n\n\n\n\n","category":"function"},{"location":"API/utility/#NeuralEstimators.IndicatorWeights","page":"Miscellaneous","title":"NeuralEstimators.IndicatorWeights","text":"IndicatorWeights(h_max, n_bins::Integer)\n(w::IndicatorWeights)(h::Matrix)\n\nFor spatial locations boldsymbols and  boldsymbolu, creates a spatial weight function defined as\n\nboldsymbolw(boldsymbols boldsymbolu) equiv (mathbbI(h in B_k)  k = 1 dots K)\n\nwhere mathbbI(cdot) denotes the indicator function,  h equiv boldsymbols - boldsymbolu  is the spatial distance between boldsymbols and  boldsymbolu, and B_k  k = 1 dots K is a set of K =n_bins equally-sized distance bins covering the spatial distances between 0 and h_max. \n\nExamples\n\nusing NeuralEstimators, GraphNeuralNetworks\n\nh_max = 1\nn_bins = 10\nw = IndicatorWeights(h_max, n_bins)\nh = rand(1, 30) # distances between 30 pairs of spatial locations \nw(h)\n\n\n\n\n\n","category":"type"},{"location":"API/utility/#NeuralEstimators.KernelWeights","page":"Miscellaneous","title":"NeuralEstimators.KernelWeights","text":"KernelWeights(h_max, n_bins::Integer)\n(w::KernelWeights)(h::Matrix)\n\nFor spatial locations boldsymbols and  boldsymbolu, creates a spatial weight function defined as\n\nboldsymbolw(boldsymbols boldsymbolu) equiv (exp(-(h - mu_k)^2  (2sigma_k^2))  k = 1 dots K)\n\nwhere h equiv boldsymbols - boldsymbolu is the spatial distance between boldsymbols and boldsymbolu, and mu_k  k = 1 dots K and sigma_k  k = 1 dots K are the means and standard deviations of the Gaussian kernels for each bin, covering the spatial distances between 0 and h_max.\n\nExamples\n\nusing NeuralEstimators, GraphNeuralNetworks\n\nh_max = 1\nn_bins = 10\nw = KernelWeights(h_max, n_bins)\nh = rand(1, 30) # distances between 30 pairs of spatial locations \nw(h)\n\n\n\n\n\n","category":"type"},{"location":"API/utility/#NeuralEstimators.maternchols","page":"Miscellaneous","title":"NeuralEstimators.maternchols","text":"maternchols(D, ρ, ν, σ² = 1; stack = true)\n\nGiven a matrix D of distances, constructs the Cholesky factor of the covariance matrix under the Matérn covariance function with range parameter ρ, smoothness parameter ν, and marginal variance σ².\n\nProviding vectors of parameters will yield a three-dimensional array of Cholesky factors (note that the vectors must of the same length, but a mix of vectors and scalars is allowed). A vector of distance matrices D may also be provided.\n\nIf stack = true, the Cholesky factors will be \"stacked\" into a three-dimensional array (this is only possible if all distance matrices in D are the same size).\n\nExamples\n\nusing NeuralEstimators\nusing LinearAlgebra: norm\nn  = 10\nS  = rand(n, 2)\nD  = [norm(sᵢ - sⱼ) for sᵢ ∈ eachrow(S), sⱼ ∈ eachrow(S)]\nρ  = [0.6, 0.5]\nν  = [0.7, 1.2]\nσ² = [0.2, 0.4]\nmaternchols(D, ρ, ν)\nmaternchols([D], ρ, ν)\nmaternchols(D, ρ, ν, σ²; stack = false)\n\nS̃  = rand(n, 2)\nD̃  = [norm(sᵢ - sⱼ) for sᵢ ∈ eachrow(S̃), sⱼ ∈ eachrow(S̃)]\nmaternchols([D, D̃], ρ, ν, σ²)\nmaternchols([D, D̃], ρ, ν, σ²; stack = false)\n\nS̃  = rand(2n, 2)\nD̃  = [norm(sᵢ - sⱼ) for sᵢ ∈ eachrow(S̃), sⱼ ∈ eachrow(S̃)]\nmaternchols([D, D̃], ρ, ν, σ²; stack = false)\n\n\n\n\n\n","category":"function"},{"location":"API/utility/#NeuralEstimators.removedata","page":"Miscellaneous","title":"NeuralEstimators.removedata","text":"removedata(Z::Array, Iᵤ::Vector{T}) where T <: Union{Integer, CartesianIndex}\nremovedata(Z::Array, p::Union{Float, Vector{Float}}; prevent_complete_missing = true)\nremovedata(Z::Array, n::Integer; fixed_pattern = false, contiguous_pattern = false)\n\nReplaces elements of Z with missing.\n\nThe simplest method accepts a vector Iᵤ that specifes the indices of the data to be removed.\n\nAlternatively, there are two methods available to randomly generate missing data.\n\nFirst, a vector p may be given that specifies the proportion of missingness for each element in the response vector. Hence, p should have length equal to the dimension of the response vector. If a single proportion is given, it will be replicated accordingly. If prevent_complete_missing = true, no replicates will contain 100% missingness (note that this can slightly alter the effective values of p).\n\nSecond, if an integer n is provided, all replicates will contain n observations after the data are removed. If fixed_pattern = true, the missingness pattern is fixed for all replicates. If contiguous_pattern = true, the data will be removed in a contiguous block based on a randomly selected starting index. \n\nThe return type is Array{Union{T, Missing}}.\n\nExamples\n\nd = 5           # dimension of each replicate\nm = 2000        # number of replicates\nZ = rand(d, m)  # simulated data\n\n# Passing a desired proportion of missingness\np = rand(d)\nremovedata(Z, p)\n\n# Passing a desired final sample size\nn = 3  # number of observed elements of each replicate: must have n <= d\nremovedata(Z, n)\n\n\n\n\n\n","category":"function"},{"location":"API/utility/#NeuralEstimators.rowwisenorm","page":"Miscellaneous","title":"NeuralEstimators.rowwisenorm","text":"rowwisenorm(A)\n\nComputes the row-wise norm of a matrix A.\n\n\n\n\n\n","category":"function"},{"location":"API/utility/#NeuralEstimators.spatialgraph","page":"Miscellaneous","title":"NeuralEstimators.spatialgraph","text":"spatialgraph(S)\nspatialgraph(S, Z)\nspatialgraph(g::GNNGraph, Z)\n\nGiven spatial data Z measured at spatial locations S, constructs a GNNGraph ready for use in a graph neural network that employs SpatialGraphConv layers. \n\nWhen m independent replicates are collected over the same set of n spatial locations,\n\nboldsymbols_1 dots boldsymbols_n subset mathcalD\n\nwhere mathcalD subset mathbbR^d denotes the spatial domain of interest,  Z should be given as an n times m matrix and S should be given as an n times d matrix.  Otherwise, when m independent replicates are collected over differing sets of spatial locations,\n\nboldsymbols_ij dots boldsymbols_in_i subset mathcalD quad i = 1 dots m\n\nZ should be given as an m-vector of n_i-vectors, and S should be given as an m-vector of n_i times d matrices.\n\nThe spatial information between neighbours is stored as an edge feature, with the specific  information controlled by the keyword arguments stationary and isotropic.  Specifically, the edge feature between node j and node j stores the spatial  distance boldsymbols_j - boldsymbols_j (if isotropic), the spatial  displacement boldsymbols_j - boldsymbols_j (if stationary), or the matrix of   locations (boldsymbols_j boldsymbols_j) (if !stationary).  \n\nAdditional keyword arguments inherit from adjacencymatrix() to determine the neighbourhood of each node, with the default being a randomly selected set of  k=30 neighbours within a disc of radius r=0.15 units.\n\nExamples\n\nusing NeuralEstimators, GraphNeuralNetworks\n\n# Number of replicates and spatial dimension\nm = 5  \nd = 2  \n\n# Spatial locations fixed for all replicates\nn = 100\nS = rand(n, d)\nZ = rand(n, m)\ng = spatialgraph(S, Z)\n\n# Spatial locations varying between replicates\nn = rand(50:100, m)\nS = rand.(n, d)\nZ = rand.(n)\ng = spatialgraph(S, Z)\n\n\n\n\n\n","category":"function"},{"location":"API/utility/#NeuralEstimators.stackarrays","page":"Miscellaneous","title":"NeuralEstimators.stackarrays","text":"stackarrays(v::Vector{<:AbstractArray}; merge::Bool = true)\n\nStack a vector of arrays v into a single higher-dimensional array.\n\nIf all arrays have the same size along the last dimension, stacks along a new final dimension. Then, if merge = true, merges the last two dimensions into one.\n\nAlternatively, if sizes differ along the last dimension, concatenates along the last dimension.\n\nExamples\n\n# Vector containing arrays of the same size:\nZ = [rand(2, 3, m) for m ∈ (1, 1)];\nstackarrays(Z)\nstackarrays(Z, merge = false)\n\n# Vector containing arrays with differing final dimension size:\nZ = [rand(2, 3, m) for m ∈ (1, 2)];\nstackarrays(Z)\n\n\n\n\n\n","category":"function"},{"location":"API/utility/#NeuralEstimators.vectotril","page":"Miscellaneous","title":"NeuralEstimators.vectotril","text":"vectotril(v; strict = false)\nvectotriu(v; strict = false)\n\nConverts a vector v of length d(d+1)2 (a triangular number) into a d  d lower or upper triangular matrix.\n\nIf strict = true, the matrix will be strictly lower or upper triangular, that is, a (d+1)  (d+1) triangular matrix with zero diagonal.\n\nNote that the triangular matrix is constructed on the CPU, but the returned matrix will be a GPU array if v is a GPU array. Note also that the return type is not of type Triangular matrix (i.e., the zeros are materialised) since Triangular matrices are not always compatible with other GPU operations.\n\nExamples\n\nusing NeuralEstimators\n\nd = 4\nn = d*(d+1)÷2\nv = collect(range(1, n))\nvectotril(v)\nvectotriu(v)\nvectotril(v; strict = true)\nvectotriu(v; strict = true)\n\n\n\n\n\n","category":"function"},{"location":"API/loss/#Loss-functions","page":"Loss functions","title":"Loss functions","text":"When training an estimator of type PointEstimator, a loss function must be specified that determines the Bayes estimator that will be approximated. In addition to the standard loss functions provided by Flux (e.g., mae, mse, which allow for the approximation of posterior medians and means, respectively), the following loss functions are provided with the package. ","category":"section"},{"location":"API/loss/#NeuralEstimators.tanhloss","page":"Loss functions","title":"NeuralEstimators.tanhloss","text":"tanhloss(θ̂, θ, κ; joint::Bool = true, scale_by_parameter_dim::Bool = true)\n\nFor κ > 0, computes the loss function given in Sainsbury-Dale et al. (2025; Eqn. 14), namely,\n\nL(hatboldsymboltheta boldsymboltheta) = tanhbig(bighatboldsymboltheta - boldsymboltheta_1kappabig)\n\nwhich yields the 0-1 loss function in the limit κ → 0.\n\nIf joint = true (default), the L₁ norm is computed over each parameter vector, so that with κ close to zero, the resulting Bayes estimator approximates the mode of the joint posterior distribution. Otherwise, if joint = false, the loss function is computed as \n\nL(hatboldsymboltheta boldsymboltheta) = sum_i=1^d tanhbig(hattheta_i - theta_ikappabig)\n\nwhere d denotes the dimension of the parameter vector boldsymboltheta. In this case, with κ close to zero, the resulting Bayes estimator approximates the vector containing the modes of the marginal posterior distributions.\n\nCompared with the kpowerloss(), which may also be used as a continuous approximation of the 0–1 loss function, the gradient of this loss is bounded as hatboldsymboltheta - boldsymboltheta_1 to 0, which can improve numerical stability during training. \n\n\n\n\n\n","category":"function"},{"location":"API/loss/#NeuralEstimators.kpowerloss","page":"Loss functions","title":"NeuralEstimators.kpowerloss","text":"kpowerloss(θ̂, θ, κ; agg = mean, safeorigin = true, ϵ = 0.1)\n\nFor κ > 0, the κ-th power absolute-distance loss function,\n\nL(hatboldsymboltheta boldsymboltheta) = hatboldsymboltheta - boldsymboltheta_1^kappa\n\ncontains the squared-error (κ = 2), absolute-error (κ = 2), and 0–1 (κ → 0) loss functions as special cases. It is Lipschitz continuous if κ = 1, convex if κ ≥ 1, and strictly convex if κ > 1. It is quasiconvex for all κ > 0.\n\nIf safeorigin = true, the loss function is modified to be piecewise, continuous, and linear in the ϵ-interval surrounding the origin, to avoid pathologies around the origin. \n\nSee also tanhloss().\n\n\n\n\n\n","category":"function"},{"location":"API/loss/#NeuralEstimators.quantileloss","page":"Loss functions","title":"NeuralEstimators.quantileloss","text":"quantileloss(θ̂, θ, τ; agg = mean)\nquantileloss(θ̂, θ, τ::Vector; agg = mean)\n\nThe asymmetric quantile loss function,\n\n  L(θ θ τ) = (θ - θ)(𝕀(θ - θ  0) - τ)\n\nwhere τ ∈ (0, 1) is a probability level and 𝕀(⋅) is the indicator function.\n\n\n\n\n\n","category":"function"},{"location":"API/loss/#NeuralEstimators.intervalscore","page":"Loss functions","title":"NeuralEstimators.intervalscore","text":"intervalscore(l, u, θ, α; agg = mean)\nintervalscore(θ̂, θ, α; agg = mean)\nintervalscore(assessment::Assessment; average_over_parameters::Bool = false, average_over_sample_sizes::Bool = true)\n\nGiven an interval [l, u] with nominal coverage 100×(1-α)%  and true value θ, the interval score (Gneiting and Raftery, 2007) is defined as\n\nS(l u θ α) = (u - l) + 2α¹(l - θ)𝕀(θ  l) + 2α¹(θ - u)𝕀(θ  u)\n\nwhere α ∈ (0, 1) and 𝕀(⋅) is the indicator function.\n\nThe method that takes a single value θ̂ assumes that θ̂ is a matrix with 2d rows, where d is the dimension of the parameter vector to make inference on. The first and second sets of d rows will be used as l and u, respectively.\n\n\n\n\n\n","category":"function"},{"location":"API/approximatedistributions/#Approximate-distributions","page":"Approximate distributions","title":"Approximate distributions","text":"When constructing a PosteriorEstimator, one must choose a family of distributions q(boldsymboltheta boldsymbolkappa), parameterized by boldsymbolkappa in mathcalK, used to approximate the posterior distribution. These families of distributions are implemented as subtypes of the abstract supertype ApproximateDistribution.","category":"section"},{"location":"API/approximatedistributions/#Distributions","page":"Approximate distributions","title":"Distributions","text":"","category":"section"},{"location":"API/approximatedistributions/#Methods","page":"Approximate distributions","title":"Methods","text":"","category":"section"},{"location":"API/approximatedistributions/#Building-blocks","page":"Approximate distributions","title":"Building blocks","text":"","category":"section"},{"location":"API/approximatedistributions/#NeuralEstimators.ApproximateDistribution","page":"Approximate distributions","title":"NeuralEstimators.ApproximateDistribution","text":"ApproximateDistribution\n\nAn abstract supertype for approximate posterior distributions used in conjunction with a PosteriorEstimator. \n\nSubtypes A <: ApproximateDistribution must implement the following methods: \n\nlogdensity(q::A, θ::AbstractMatrix, t::AbstractMatrix) \nUsed during training and therefore must support automatic differentiation.\nθ is a d × K matrix of parameter vectors.\nt is a dstar × K matrix of learned summary statistics obtained by applying the neural network in the PosteriorEstimator to a collection of K data sets. \nShould return a 1 × K matrix, where each entry is the log density log q(θₖ | tₖ) for the k-th data set evaluated at the k-th parameter vector θ[:, k].\nsampleposterior(q::A, t::AbstractMatrix, N::Integer)\nUsed during inference and therefore does not need to be differentiable.\nShould return a Vector of length K, where each element is a d × N matrix containing N samples from the approximate posterior q(θ | tₖ) for the k-th data set.\n\n\n\n\n\n","category":"type"},{"location":"API/approximatedistributions/#NeuralEstimators.GaussianMixture","page":"Approximate distributions","title":"NeuralEstimators.GaussianMixture","text":"GaussianMixture <: ApproximateDistribution\nGaussianMixture(d::Integer, dstar::Integer; num_components::Integer = 10, kwargs...)\n\nA mixture of Gaussian distributions for amortised posterior inference, where d is the dimension of the parameter vector. \n\nThe density of the distribution is: \n\nq(boldsymboltheta boldsymbolkappa) = sum_j=1^J pi_j cdot mathcalN(boldsymboltheta boldsymbolmu_j boldsymbolSigma_j) \n\nwhere the parameters boldsymbolkappa comprise the mixture weights pi_j in 0 1 subject to sum_j=1^J pi_j = 1, the mean vector boldsymbolmu_j of each component, and the variance parameters of the diagonal covariance matrix boldsymbolSigma_j.\n\nWhen using a GaussianMixture as the approximate distribution of a PosteriorEstimator,  the neural network should be a mapping from the sample space to mathbbR^d^*,  where d^* is an appropriate number of summary statistics for the parameter vector boldsymboltheta. The summary statistics are then mapped to the mixture parameters using a conventional multilayer perceptron (MLP) with approporiately chosen output activation functions (e.g., softmax for the mixture weights, softplus for the variance parameters).\n\nKeyword arguments\n\nnum_components::Integer = 10: number of components in the mixture. \nkwargs: additional keyword arguments passed to MLP. \n\n\n\n\n\n","category":"type"},{"location":"API/approximatedistributions/#NeuralEstimators.NormalisingFlow","page":"Approximate distributions","title":"NeuralEstimators.NormalisingFlow","text":"NormalisingFlow <: ApproximateDistribution\nNormalisingFlow(d::Integer, dstar::Integer; num_coupling_layers::Integer = 6, kwargs...)\n\nA normalising flow for amortised posterior inference (e.g., Ardizzone et al., 2019; Radev et al., 2022), where d is the dimension of  the parameter vector and dstar is the dimension of the summary statistics for the data. \n\nNormalising flows are diffeomorphisms (i.e., invertible, differentiable transformations with differentiable inverses) that map a simple base distribution (e.g., standard Gaussian) to a more complex target distribution (e.g., the posterior). They achieve this by applying a sequence of learned transformations, the forms of which are chosen to be invertible and allow for tractable density computation via the change of variables formula. This allows for efficient density evaluation during the training stage, and efficient sampling during the inference stage. For further details, see the reviews by Kobyzev et al. (2020) and Papamakarios (2021).\n\nNormalisingFlow uses affine coupling blocks (see AffineCouplingBlock), with activation normalisation (Kingma and Dhariwal, 2018) and permutations used between each block. The base distribution is taken to be a standard multivariate Gaussian distribution. \n\nWhen using a NormalisingFlow as the approximate distribution of a PosteriorEstimator,  the neural network should be a mapping from the sample space to mathbbR^d^*,  where d^* is an appropriate number of summary statistics for the given parameter vector (e.g., d^* = d). The summary statistics are then mapped to the parameters of the affine coupling blocks using conventional multilayer perceptrons (see AffineCouplingBlock).\n\nKeyword arguments\n\nnum_coupling_layers::Integer = 6: number of coupling layers. \nkwargs: additional keyword arguments passed to AffineCouplingBlock. \n\n\n\n\n\n","category":"type"},{"location":"API/approximatedistributions/#NeuralEstimators.numdistributionalparams","page":"Approximate distributions","title":"NeuralEstimators.numdistributionalparams","text":"numdistributionalparams(q::ApproximateDistribution)\nnumdistributionalparams(estimator::PosteriorEstimator)\n\nThe number of distributional parameters (i.e., the dimension of the space mathcalK of approximate-distribution parameters boldsymbolkappa). \n\n\n\n\n\n","category":"function"},{"location":"API/approximatedistributions/#NeuralEstimators.AffineCouplingBlock","page":"Approximate distributions","title":"NeuralEstimators.AffineCouplingBlock","text":"AffineCouplingBlock(κ₁::MLP, κ₂::MLP)\nAffineCouplingBlock(d₁::Integer, dstar::Integer, d₂; kwargs...)\n\nAn affine coupling block used in a NormalisingFlow. \n\nAn affine coupling block splits its input boldsymboltheta into two disjoint components, boldsymboltheta_1 and boldsymboltheta_2, with dimensions d_1 and d_2, respectively. The block then applies the following transformation: \n\nbeginaligned\n    tildeboldsymboltheta_1 = boldsymboltheta_1\n    tildeboldsymboltheta_2 = boldsymboltheta_2 odot expboldsymbolkappa_boldsymbolgamma1(tildeboldsymboltheta_1 boldsymbolT(boldsymbolZ)) + boldsymbolkappa_boldsymbolgamma2(tildeboldsymboltheta_1 boldsymbolT(boldsymbolZ))\nendaligned\n\nwhere boldsymbolkappa_boldsymbolgamma1(cdot) and boldsymbolkappa_boldsymbolgamma2(cdot) are generic, non-invertible multilayer perceptrons (MLPs) that are functions of both the (transformed) first input component tildeboldsymboltheta_1 and the learned d^*-dimensional summary statistics boldsymbolT(boldsymbolZ) (see PosteriorEstimator). \n\nTo prevent numerical overflows and stabilise the training of the model, the scaling factors boldsymbolkappa_boldsymbolgamma1(cdot) are clamped using the function \n\nf(boldsymbols) = frac2cpitan^-1(fracboldsymbolsc)\n\nwhere c = 19 is a fixed clamping threshold. This transformation ensures that the scaling factors do not grow excessively large.\n\nAdditional keyword arguments kwargs are passed to the MLP constructor when creating κ₁ and κ₂. \n\n\n\n\n\n","category":"type"},{"location":"API/architectures/#Neural-network-building-blocks","page":"Neural-network building blocks","title":"Neural-network building blocks","text":"As discussed in the Overview, any Flux model can be used to construct a neural network when using the package. In addition to the standard Flux layers and architectures, the following components can be useful.","category":"section"},{"location":"API/architectures/#Modules","page":"Neural-network building blocks","title":"Modules","text":"The structures listed below are often useful when constructing neural estimators. In particular, DeepSet provides a convenient wrapper for embedding standard neural networks (e.g., MLPs, CNNs, GNNs) into a framework suited to making inference with an arbitrary number of independent replicates. ","category":"section"},{"location":"API/architectures/#User-defined-summary-statistics","page":"Neural-network building blocks","title":"User-defined summary statistics","text":"Order = [:type, :function]\nPages   = [\"summarystatistics.md\"]\n\nThe following functions correspond to summary statistics that are often useful as user-defined summaries in DeepSet objects.","category":"section"},{"location":"API/architectures/#Layers","page":"Neural-network building blocks","title":"Layers","text":"In addition to the built-in layers provided by Flux, the following layers may be used when building a neural-network architecture.","category":"section"},{"location":"API/architectures/#Output-layers","page":"Neural-network building blocks","title":"Output layers","text":"Order = [:type, :function]\nPages   = [\"activationfunctions.md\"]\n\nIn addition to the standard activation functions provided by Flux (e.g., relu, softplus), the following layers can be used at the end of an architecture to ensure valid estimates for certain models. Note that the Flux layer Parallel can be useful for applying several different parameter constraints, as shown in the Univariate data example.\n\nnote: Layers vs. activation functions\nAlthough we may conceptualise the following types as \"output activation functions\", they should be treated as separate layers included in the final stage of a Flux Chain(). In particular, they cannot be used as the activation function of a Dense layer. ","category":"section"},{"location":"API/architectures/#NeuralEstimators.DeepSet","page":"Neural-network building blocks","title":"NeuralEstimators.DeepSet","text":"DeepSet(ψ, ϕ, a = mean; S = nothing)\n(ds::DeepSet)(Z::Vector{A}) where A <: Any\n(ds::DeepSet)(tuple::Tuple{Vector{A}, Vector{Vector}}) where A <: Any\n\nThe DeepSets representation (Zaheer et al., 2017; Sainsbury-Dale et al., 2024),\n\nhatboldsymboltheta(mathbfZ) = boldsymbolphi(mathbfT(mathbfZ)) quad\nmathbfT(mathbfZ) = mathbfa(boldsymbolpsi(mathbfZ_i)  i = 1 dots m)\n\nwhere 𝐙 ≡ (𝐙₁', …, 𝐙ₘ')' are independent replicates of data,  ψ and ϕ are neural networks, and a is a permutation-invariant aggregation function. \n\nThe function a must operate on arrays and have a keyword argument dims for  specifying the dimension of aggregation (e.g., mean, sum, maximum, minimum, logsumexp).\n\nDeepSet objects act on data of type Vector{A}, where each element of the vector is associated with one data set (i.e., one set of independent replicates), and where A depends on the chosen architecture for ψ.  Independent replicates within each data set are stored in the batch dimension.  For example, data collected over a two-dimensional grid and ψ a CNN, A should be a 4-dimensional array,  with replicates stored in the 4ᵗʰ dimension. \n\nFor computational efficiency,  array data are first concatenated along their final dimension  (i.e., the replicates dimension) before being passed into the inner network ψ,  thereby ensuring that ψ is applied to a single large array, rather than multiple small ones. \n\nFixed (non-trainable) transformations of the data can be incorporated alongside the learned summaries via the S argument:\n\nhatboldsymboltheta(mathbfZ) = boldsymbolphi((mathbfT(mathbfZ) mathbfS(mathbfZ)))\n\nwhere S is a function (or vector of functions) that maps data to a vector of fixed summary statistics. These are not differentiated through during training. In the case that ψ is set to nothing, only the fixed summaries will be used. For the common case where summary statistics are precomputed and stored alongside the data, see DataSet as an alternative approach.\n\nSet-level inputs (e.g., covariates) 𝐗 can be passed directly into the outer network ϕ in the following manner: \n\nhatboldsymboltheta(mathbfZ) = boldsymbolphi((mathbfT(mathbfZ) mathbfX))\n\nor, when fixed transformations are also used,\n\nhatboldsymboltheta(mathbfZ) = boldsymbolphi((mathbfT(mathbfZ) mathbfS(mathbfZ) mathbfX))\n\nThis is done by calling the DeepSet object on a Tuple{Vector{A}, Vector{Vector}}, where the first element of the tuple contains a vector of data sets and the second element contains a vector of set-level inputs (i.e., one vector for each data set).\n\nExamples\n\nusing NeuralEstimators, Flux\n\n# Two data sets containing 3 and 4 replicates\nd = 5  # number of parameters in the model\nn = 10 # dimension of each replicate\nZ = [rand32(n, m) for m ∈ (3, 4)]\n\n# Construct DeepSet object\ndₜ = 16  # dimension of neural summary statistic\nw  = 32  # width of hidden layers\nψ  = Chain(Dense(n, w, relu), Dense(w, dₜ, relu))\nϕ  = Chain(Dense(dₜ, w, relu), Dense(w, d))\nds = DeepSet(ψ, ϕ)\n\n# Apply DeepSet object to data\nds(Z)\n\n# With fixed transformations S\ndₛ = 1   # dimension of fixed summary statistic\nϕ  = Chain(Dense(dₜ + dₛ, w, relu), Dense(w, d))\nds = DeepSet(ψ, ϕ; S = logsamplesize)\nds(Z)\n\n# With set-level inputs \ndₓ = 2 # dimension of set-level inputs \nϕ  = Chain(Dense(dₜ + dₓ, w, relu), Dense(w, d))\nds = DeepSet(ψ, ϕ)\nX  = [rand32(dₓ) for _ ∈ eachindex(Z)]\nds((Z, X))\n\n\n\n\n\n","category":"type"},{"location":"API/architectures/#NeuralEstimators.GNNSummary","page":"Neural-network building blocks","title":"NeuralEstimators.GNNSummary","text":"GNNSummary(propagation, readout)\n\nA graph neural network (GNN) module designed to serve as the inner network ψ in the DeepSet representation when the data are graphical (e.g., irregularly observed spatial data).\n\nThe propagation module transforms graph data into a set of hidden-feature graphs. The readout module aggregates these feature graphs into a single hidden feature vector of fixed length. The network ψ is then defined as the composition of the propagation and readout modules.\n\nThe data should be stored as a GNNGraph or Vector{GNNGraph}, where each graph is associated with a single parameter vector. The graphs may contain subgraphs corresponding to independent replicates.\n\nExamples\n\nusing NeuralEstimators, Flux, GraphNeuralNetworks\nusing Statistics: mean\n\n# Spatial data\nn = 100                # number of spatial locations\nm = 50                 # number of independent replicates\nS = rand(n, 2)         # spatial locations\nZ = rand(n, m)         # observed data\ng = spatialgraph(S, Z) # construct the graph\n\n# Propagation module\nnₕ = 32    # dimension of node feature vectors\npropagation = Chain(SpatialGraphConv(1 => nₕ), SpatialGraphConv(nₕ => nₕ))\n\n# Readout module\nreadout = GlobalPool(mean)\n\n# Inner network\nψ = GNNSummary(propagation, readout)\n\n# Outer network\nd = 3     # number of parameters\nw = 64    # width of hidden layer\nϕ = Chain(Dense(nₕ, w, relu), Dense(w, d))\n\n# DeepSet object \nds = DeepSet(ψ, ϕ)\n\n# Apply to data \nds(g)        # single graph with subgraphs corresponding to independent replicates\nds([g, g])   # vector of graphs, corresponding to multiple data sets \n\n\n\n\n\n","category":"type"},{"location":"API/architectures/#NeuralEstimators.MLP","page":"Neural-network building blocks","title":"NeuralEstimators.MLP","text":"MLP(in::Integer, out::Integer; kwargs...)\n(mlp::MLP)(x)\n(mlp::MLP)(x, y)\n\nA traditional fully-connected multilayer perceptron (MLP) with input dimension in and output dimension out.\n\nThe method (mlp::MLP)(x, y) concatenates x and y along their first dimension before passing the result through the neural network. This functionality is used in constructs such as AffineCouplingBlock. \n\nKeyword arguments\n\ndepth::Integer = 2: the number of hidden layers.\nwidth::Integer = 128: the width of each hidden layer.\nactivation::Function = relu: the (non-linear) activation function used in each hidden layer.\noutput_activation = identity: the activation function used in the output layer.\nfinal_layer = nothing: an optional final layer to append to the network. If provided, it must accept width inputs. When set, output_activation is ignored and replaced with identity. The effective depth of the network becomes depth + 1.\n\n\n\n\n\n","category":"type"},{"location":"API/architectures/#NeuralEstimators.samplesize","page":"Neural-network building blocks","title":"NeuralEstimators.samplesize","text":"samplesize(Z)\n\nComputes the number of independent replicates in the data set Z. \n\nNote that this function is a wrapper around numberreplicates with return type equal to the eltype of Z.\n\n\n\n\n\n","category":"function"},{"location":"API/architectures/#NeuralEstimators.logsamplesize","page":"Neural-network building blocks","title":"NeuralEstimators.logsamplesize","text":"logsamplesize(Z)\n\nComputes the log of the number of independent replicates in the data set Z. \n\n\n\n\n\n","category":"function"},{"location":"API/architectures/#NeuralEstimators.invsqrtsamplesize","page":"Neural-network building blocks","title":"NeuralEstimators.invsqrtsamplesize","text":"invsqrtsamplesize(Z)\n\nComputes the inverse of the square root of the number of independent replicates in the data set Z. \n\n\n\n\n\n","category":"function"},{"location":"API/architectures/#NeuralEstimators.samplecorrelation","page":"Neural-network building blocks","title":"NeuralEstimators.samplecorrelation","text":"samplecorrelation(Z::AbstractArray)\n\nComputes the sample correlation matrix, R̂, and returns the vectorised strict lower triangle of R̂.\n\nExamples\n\n# 5 independent replicates of a 3-dimensional vector\nz = rand(3, 5)\nsamplecorrelation(z)\n\n\n\n\n\n","category":"function"},{"location":"API/architectures/#NeuralEstimators.samplecovariance","page":"Neural-network building blocks","title":"NeuralEstimators.samplecovariance","text":"samplecovariance(Z::AbstractArray)\n\nComputes the sample covariance matrix, Σ̂, and returns the vectorised lower triangle of Σ̂.\n\nExamples\n\n# 5 independent replicates of a 3-dimensional vector\nz = rand(3, 5)\nsamplecovariance(z)\n\n\n\n\n\n","category":"function"},{"location":"API/architectures/#NeuralEstimators.NeighbourhoodVariogram","page":"Neural-network building blocks","title":"NeuralEstimators.NeighbourhoodVariogram","text":"NeighbourhoodVariogram(h_max, n_bins) \n(l::NeighbourhoodVariogram)(g::GNNGraph)\n\nComputes the empirical variogram, \n\nhatgamma(h pm delta) = frac12N(h pm delta) sum_(ij) in N(h pm delta) (Z_i - Z_j)^2\n\nwhere N(h pm delta) equiv left(ij)  boldsymbols_i - boldsymbols_j in (h-delta h+delta)right  is the set of pairs of locations separated by a distance within (h-delta h+delta), and cdot denotes set cardinality. \n\nThe distance bins are constructed to have constant width 2delta, chosen based on the maximum distance  h_max to be considered, and the specified number of bins n_bins. \n\nThe input type is a GNNGraph, and the empirical variogram is computed based on the corresponding graph structure.  Specifically, only locations that are considered neighbours will be used when computing the empirical variogram. \n\nExamples\n\nusing NeuralEstimators, GraphNeuralNetworks, Distances, LinearAlgebra\n  \n# Simulate Gaussian spatial data with exponential covariance function \nθ = 0.1                                 # true range parameter \nn = 250                                 # number of spatial locations \nS = rand(n, 2)                          # spatial locations \nD = pairwise(Euclidean(), S, dims = 1)  # distance matrix \nΣ = exp.(-D ./ θ)                       # covariance matrix \nL = cholesky(Symmetric(Σ)).L            # Cholesky factor \nm = 5                                   # number of independent replicates \nZ = L * randn(n, m)                     # simulated data \n\n# Construct the spatial graph \nr = 0.15                                # radius of neighbourhood set\ng = spatialgraph(S, Z, r = r)\n\n# Construct the variogram object with 10 bins\nnv = NeighbourhoodVariogram(r, 10) \n\n# Compute the empirical variogram \nnv(g)\n\n\n\n\n\n","category":"type"},{"location":"API/architectures/#NeuralEstimators.DensePositive","page":"Neural-network building blocks","title":"NeuralEstimators.DensePositive","text":"DensePositive(layer::Dense; g::Function = relu, last_only::Bool = false)\n\nWrapper around the standard Dense layer that ensures positive weights (biases are left unconstrained).\n\nThis layer can be useful for constucting (partially) monotonic neural networks. \n\nExamples\n\nusing NeuralEstimators, Flux\n\nl = DensePositive(Dense(5 => 2))\nx = rand32(5, 64)\nl(x)\n\n\n\n\n\n","category":"type"},{"location":"API/architectures/#NeuralEstimators.PowerDifference","page":"Neural-network building blocks","title":"NeuralEstimators.PowerDifference","text":"PowerDifference(a, b)\n\nFunction f(x y) = ax - (1-a)y^b for trainable parameters a ∈ [0, 1] and b > 0.\n\nExamples\n\nusing NeuralEstimators, Flux\n\n# Generate some data\nd = 5\nK = 10000\nX = randn32(d, K)\nY = randn32(d, K)\nXY = (X, Y)\na = 0.2f0\nb = 1.3f0\nZ = (abs.(a .* X - (1 .- a) .* Y)).^b\n\n# Initialise layer\nf = PowerDifference([0.5f0], [2.0f0])\n\n# Optimise the layer\nloader = Flux.DataLoader((XY, Z), batchsize=32, shuffle=false)\noptim = Flux.setup(Flux.Adam(0.01), f)\nfor epoch in 1:100\n    for (xy, z) in loader\n        loss, grads = Flux.withgradient(f) do m\n            Flux.mae(m(xy), z)\n        end\n        Flux.update!(optim, f, grads[1])\n    end\nend\n\n# Estimates of a and b\nf.a\nf.b\n\n\n\n\n\n","category":"type"},{"location":"API/architectures/#NeuralEstimators.ResidualBlock","page":"Neural-network building blocks","title":"NeuralEstimators.ResidualBlock","text":"ResidualBlock(filter, in => out; stride = 1)\n\nBasic residual block (see here), consisting of two sequential convolutional layers and a skip (shortcut) connection that connects the input of the block directly to the output, facilitating the training of deep networks.\n\nExamples\n\nusing NeuralEstimators\nz = rand(16, 16, 1, 1)\nb = ResidualBlock((3, 3), 1 => 32)\nb(z)\n\n\n\n\n\n","category":"type"},{"location":"API/architectures/#NeuralEstimators.SpatialGraphConv","page":"Neural-network building blocks","title":"NeuralEstimators.SpatialGraphConv","text":"SpatialGraphConv(in => out, g=relu; args...)\n\nImplements a spatial graph convolution for isotropic spatial processes (Sainsbury-Dale et al., 2025), \n\n boldsymbolh^(l)_j =\n gBig(\n boldsymbolGamma_1^(l) boldsymbolh^(l-1)_j\n +\n boldsymbolGamma_2^(l) barboldsymbolh^(l)_j\n +\n boldsymbolgamma^(l)\n Big)\n quad\n barboldsymbolh^(l)_j = sum_j in mathcalN(j)boldsymbolw^(l)(boldsymbols_j - boldsymbols_j) odot f^(l)(boldsymbolh^(l-1)_j boldsymbolh^(l-1)_j)\n\nwhere boldsymbolh^(l)_j is the hidden feature vector at location boldsymbols_j at layer l, g(cdot) is a non-linear activation function applied elementwise, boldsymbolGamma_1^(l) and boldsymbolGamma_2^(l) are trainable parameter matrices, boldsymbolgamma^(l) is a trainable bias vector, mathcalN(j) denotes the indices of neighbours of boldsymbols_j, boldsymbolw^(l)(cdot) is a (learnable) spatial weighting function, odot denotes elementwise multiplication,  and f^(l)(cdot cdot) is a (learnable) function. \n\nBy default, the function f^(l)(cdot cdot) is modelled using a PowerDifference function.  One may alternatively employ a nonlearnable function, for example, f = (hᵢ, hⱼ) -> (hᵢ - hⱼ).^2,  specified through the keyword argument f.  \n\nThe spatial distances between locations must be stored as an edge feature, as facilitated by spatialgraph().  The input to boldsymbolw^(l)(cdot) is a 1 times n matrix (i.e., a row vector) of spatial distances.  The output of boldsymbolw^(l)(cdot) must be either a scalar; a vector of the same dimension as the feature vectors of the previous layer;  or, if the features vectors of the previous layer are scalars, a vector of arbitrary dimension.  To promote identifiability, the weights are normalised to sum to one (row-wise) within each neighbourhood set.  By default, boldsymbolw^(l)(cdot) is taken to be a multilayer perceptron with a single hidden layer,  although a custom choice for this function can be provided using the keyword argument w. \n\nArguments\n\nin: dimension of input features.\nout: dimension of output features.\ng = relu: activation function.\nbias = true: add learnable bias?\ninit = glorot_uniform: initialiser for boldsymbolGamma_1^(l), boldsymbolGamma_2^(l), and boldsymbolgamma^(l). \nf = nothing\nw = nothing \nw_width = 128 (applicable only if w = nothing): the width of the hidden layer in the MLP used to model boldsymbolw^(l)(cdot cdot). \nw_out = in (applicable only if w = nothing): the output dimension of boldsymbolw^(l)(cdot cdot).  \n\nExamples\n\nusing NeuralEstimators, Flux, GraphNeuralNetworks\n\n# Toy spatial data\nn = 250                # number of spatial locations\nm = 5                  # number of independent replicates\nS = rand(n, 2)         # spatial locations\nZ = rand(n, m)         # data\ng = spatialgraph(S, Z) # construct the graph\n\n# Construct and apply spatial graph convolution layer\nl = SpatialGraphConv(1 => 10)\nl(g)\n\n\n\n\n\n","category":"type"},{"location":"API/architectures/#NeuralEstimators.Compress","page":"Neural-network building blocks","title":"NeuralEstimators.Compress","text":"Compress(a, b, k = 1)\n\nLayer that compresses its input to be within the range a and b, where each element of a is less than the corresponding element of b.\n\nThe layer uses a logistic function,\n\nl(θ) = a + fracb - a1 + e^-kθ\n\nwhere the arguments a and b together combine to shift and scale the logistic function to the range (a, b), and the growth rate k controls the steepness of the curve.\n\nThe logistic function given here contains an additional parameter, θ₀, which is the input value corresponding to the functions midpoint. In Compress, we fix θ₀ = 0, since the output of a randomly initialised neural network is typically around zero.\n\nExamples\n\nusing NeuralEstimators, Flux\n\na = [25, 0.5, -pi/2]\nb = [500, 2.5, 0]\np = length(a)\nK = 100\nθ = randn(p, K)\nl = Compress(a, b)\nl(θ)\n\nn = 20\nθ̂ = Chain(Dense(n, p), l)\nZ = randn(n, K)\nθ̂(Z)\n\n\n\n\n\n","category":"type"},{"location":"API/architectures/#NeuralEstimators.CorrelationMatrix","page":"Neural-network building blocks","title":"NeuralEstimators.CorrelationMatrix","text":"CorrelationMatrix(d)\n(object::CorrelationMatrix)(x::Matrix, cholesky::Bool = false)\n\nTransforms a vector 𝐯 ∈ ℝᵈ to the parameters of an unconstrained d×d correlation matrix or, if cholesky = true, the lower Cholesky factor of an unconstrained d×d correlation matrix.\n\nThe expected input is a Matrix with T(d-1) = (d-1)d÷2 rows, where T(d-1) is the (d-1)th triangular number (the number of free parameters in an unconstrained d×d correlation matrix), and the output is a Matrix of the same dimension. The columns of the input and output matrices correspond to independent parameter configurations (i.e., different correlation matrices).\n\nInternally, the layer constructs a valid Cholesky factor 𝐋 for a correlation matrix, and then extracts the strict lower triangle from the correlation matrix 𝐑 = 𝐋𝐋'. The lower triangle is extracted and vectorised in line with Julia's column-major ordering: for example, when modelling the correlation matrix\n\nbeginbmatrix\n1    R₁₂   R₁₃ \nR₂₁  1     R₂₃\nR₃₁  R₃₂  1\nendbmatrix\n\nthe rows of the matrix returned by a CorrelationMatrix layer are ordered as\n\nbeginbmatrix\nR₂₁ \nR₃₁ \nR₃₂ \nendbmatrix\n\nwhich means that the output can easily be transformed into the implied correlation matrices using vectotril and Symmetric.\n\nSee also CovarianceMatrix.\n\nExamples\n\nusing NeuralEstimators, LinearAlgebra, Flux\n\nd  = 4\nl  = CorrelationMatrix(d)\np  = (d-1)*d÷2\nθ  = randn(p, 100)\n\n# Returns a matrix of parameters, which can be converted to correlation matrices\nR = l(θ)\nR = map(eachcol(R)) do r\n\tR = Symmetric(cpu(vectotril(r, strict = true)), :L)\n\tR[diagind(R)] .= 1\n\tR\nend\n\n# Obtain the Cholesky factor directly\nL = l(θ, true)\nL = map(eachcol(L)) do x\n\t# Only the strict lower diagonal elements are returned\n\tL = LowerTriangular(cpu(vectotril(x, strict = true)))\n\n\t# Diagonal elements are determined under the constraint diag(L*L') = 𝟏\n\tL[diagind(L)] .= sqrt.(1 .- rowwisenorm(L).^2)\n\tL\nend\nL[1] * L[1]'\n\n\n\n\n\n","category":"type"},{"location":"API/architectures/#NeuralEstimators.CovarianceMatrix","page":"Neural-network building blocks","title":"NeuralEstimators.CovarianceMatrix","text":"CovarianceMatrix(d)\n(object::CovarianceMatrix)(x::Matrix, cholesky::Bool = false)\n\nTransforms a vector 𝐯 ∈ ℝᵈ to the parameters of an unconstrained d×d covariance matrix or, if cholesky = true, the lower Cholesky factor of an unconstrained d×d covariance matrix.\n\nThe expected input is a Matrix with T(d) = d(d+1)÷2 rows, where T(d) is the dth triangular number (the number of free parameters in an unconstrained d×d covariance matrix), and the output is a Matrix of the same dimension. The columns of the input and output matrices correspond to independent parameter configurations (i.e., different covariance matrices).\n\nInternally, the layer constructs a valid Cholesky factor 𝐋 and then extracts the lower triangle from the positive-definite covariance matrix 𝚺 = 𝐋𝐋'. The lower triangle is extracted and vectorised in line with Julia's column-major ordering: for example, when modelling the covariance matrix\n\nbeginbmatrix\nΣ₁₁  Σ₁₂  Σ₁₃ \nΣ₂₁  Σ₂₂  Σ₂₃ \nΣ₃₁  Σ₃₂  Σ₃₃ \nendbmatrix\n\nthe rows of the matrix returned by a CovarianceMatrix are ordered as\n\nbeginbmatrix\nΣ₁₁ \nΣ₂₁ \nΣ₃₁ \nΣ₂₂ \nΣ₃₂ \nΣ₃₃ \nendbmatrix\n\nwhich means that the output can easily be transformed into the implied covariance matrices using vectotril and Symmetric.\n\nSee also CorrelationMatrix.\n\nExamples\n\nusing NeuralEstimators, Flux, LinearAlgebra\n\nd = 4\nl = CovarianceMatrix(d)\np = d*(d+1)÷2\nθ = randn(p, 50)\n\n# Returns a matrix of parameters, which can be converted to covariance matrices\nΣ = l(θ)\nΣ = [Symmetric(cpu(vectotril(x)), :L) for x ∈ eachcol(Σ)]\n\n# Obtain the Cholesky factor directly\nL = l(θ, true)\nL = [LowerTriangular(cpu(vectotril(x))) for x ∈ eachcol(L)]\nL[1] * L[1]'\n\n\n\n\n\n","category":"type"},{"location":"#NeuralEstimators","page":"NeuralEstimators","title":"NeuralEstimators","text":"NeuralEstimators facilitates a suite of neural methods for parameter inference in scenarios where simulation from the model is feasible. These methods are likelihood-free and amortised, in the sense that, once the neural networks are trained on simulated data, they enable rapid inference across arbitrarily many observed data sets in a fraction of the time required by conventional approaches. \n\nThe package supports neural Bayes estimators, which transform data into point summaries of the posterior distribution; neural posterior estimators, which perform approximate posterior inference via KL-divergence minimisation; and neural ratio estimators, which approximate the likelihood-to-evidence ratio and thereby enable frequentist or Bayesian inference through various downstream algorithms.\n\nUser-friendliness is a central focus of the package, which is designed to minimise \"boilerplate\" code while preserving complete flexibility in the neural-network architecture and other workflow components. The package accommodates any model for which simulation is feasible by allowing users to define their model implicitly through simulated data. A convenient interface for R users is available on CRAN.\n\nOnce familiar with the Methodology, see the Overview of the package workflow and the Examples, or refer to the Quick start section below.","category":"section"},{"location":"#Installation","page":"NeuralEstimators","title":"Installation","text":"To install the package, please first install the current stable release of Julia. Then, one may install the current stable version of the package using the following command inside Julia:\n\nusing Pkg; Pkg.add(\"NeuralEstimators\")\n\nAlternatively, one may install the current development version using the command:\n\nusing Pkg; Pkg.add(url = \"https://github.com/msainsburydale/NeuralEstimators.jl\")","category":"section"},{"location":"#Quick-start","page":"NeuralEstimators","title":"Quick start","text":"In the following minimal example, we develop a neural Bayes estimator for boldsymboltheta equiv (mu sigma) from data boldsymbolZ equiv (Z_1 dots Z_m), where each Z_i oversetmathrmiidsim N(mu sigma^2). \n\nusing NeuralEstimators, Flux\n\n# Priors μ,σ ~ U(0, 1) and data Zᵢ|μ,σ ~ N(μ, σ²), i = 1,…, m\nd = 2    # dimension of the parameter vector θ\nn = 1    # dimension of each data replicate Zᵢ\nsample(K) = rand(d, K) \nsimulate(θ, m = 100) = [ϑ[1] .+ ϑ[2] * randn(n, m) for ϑ ∈ eachcol(θ)]  \n\n# Neural network, based on the DeepSets architecture\nw = 128  # width of each hidden layer \nψ = Chain(Dense(n, w, relu), Dense(w, w, relu))\nϕ = Chain(Dense(w, w, relu), Dense(w, d))\nnetwork = DeepSet(ψ, ϕ)\n\n# Initialise a neural point estimator\nestimator = PointEstimator(network) \n\n# Train the estimator\nestimator = train(estimator, sample, simulate, epochs = 20)\n\n# Assess the estimator\nθ_test = sample(1000)\nZ_test = simulate(θ_test)\nassessment = assess(estimator, θ_test, Z_test)\nbias(assessment)   # μ = 0.001, σ = 0.001\nrmse(assessment)   # μ = 0.05,  σ = 0.04\n\n# Apply the estimator to observed data\nθ = [0.8 0.1]'          # true parameters\nZ = simulate(θ)         # \"observed\" data\nestimate(estimator, Z)  # point estimate: μ̂ = 0.797, σ̂ = 0.087","category":"section"},{"location":"#Contributing","page":"NeuralEstimators","title":"Contributing","text":"To get started, see CONTRIBUTING.md for an overview of the code structure, development workflow, and how to submit contributions. A list of planned improvements is available in TODO.md, and instructions for contributing to the documentation can be found in docs/README.md. \n\nIf you encounter a bug or have a suggestion, please feel free to open an issue or submit a pull request.","category":"section"},{"location":"#Supporting-and-citing","page":"NeuralEstimators","title":"Supporting and citing","text":"This software was developed as part of academic research. If you would like to support it, please star the repository. If you use it in your research or other activities, please also use the following citation.\n\n@misc{NeuralEstimators.jl,\n  title = {{NeuralEstimators.jl}: A {J}ulia package for efficient simulation-based inference using neural networks},\n  author = {Sainsbury-Dale, Matthew},\n  year = {2026},\n  note = {Version 0.2.0},\n  howpublished = {\\url{https://github.com/msainsburydale/NeuralEstimators.jl}}\n}","category":"section"},{"location":"#Papers-using-NeuralEstimators","page":"NeuralEstimators","title":"Papers using NeuralEstimators","text":"Likelihood-free parameter estimation with neural Bayes estimators [paper] [code]\nNeural methods for amortized inference [paper][code]\nNeural Bayes estimators for irregular spatial data using graph neural networks [paper][code]\nNeural Bayes estimators for censored inference with peaks-over-threshold models [paper] [code]\nNeural parameter estimation with incomplete data [paper][code]\nNeural Bayes inference for complex bivariate extremal dependence models [paper][code]\nFast likelihood-free parameter estimation for Lévy processes [paper]","category":"section"}]
}
